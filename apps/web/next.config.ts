import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* Next.js 16 defaults — Turbopack is the default bundler.
   * Phase 0: no custom config required. Phase 1+ may add:
   *   - images.remotePatterns (when receipts go through a CDN)
   *   - rewrites/redirects for marketing pages
   *   - experimental.cacheComponents
   */
};

export default nextConfig;
