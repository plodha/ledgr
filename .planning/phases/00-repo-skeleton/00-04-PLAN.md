---
phase: 00-repo-skeleton
plan: 04
type: execute
wave: 3
depends_on: ["00-02"]
files_modified:
  - apps/web/.nvmrc
  - apps/web/package.json
  - apps/web/pnpm-lock.yaml
  - apps/web/tsconfig.json
  - apps/web/next.config.ts
  - apps/web/eslint.config.mjs
  - apps/web/postcss.config.mjs
  - apps/web/components.json
  - apps/web/src/app/layout.tsx
  - apps/web/src/app/page.tsx
  - apps/web/src/app/globals.css
  - apps/web/Dockerfile
  - apps/web/.dockerignore
  - packages/shared/types.ts
autonomous: true
requirements:
  - INFRA-01

must_haves:
  truths:
    - "apps/web is a Next.js 16.2.6 project scaffolded via `pnpm create next-app@16.2.6 web --yes` (TypeScript, Tailwind 4, ESLint flat, App Router, Turbopack, @/* alias)"
    - "shadcn/ui is initialized (components.json exists with cssVariables=true, tailwind.config empty for Tailwind 4)"
    - "TanStack Query v5, Zustand v5, React Hook Form v7, Zod v4, @hookform/resolvers v5 are installed (Phase 1+ uses them; Phase 0 only locks versions)"
    - "src/app/page.tsx is an async Server Component that fetches API_URL_INTERNAL/health (fallback NEXT_PUBLIC_API_URL/health) and renders the PRANAV title + the health JSON (or 'API unavailable' on fetch failure)"
    - "apps/web/Dockerfile is a multi-stage build on node:22-alpine that produces a runnable image (pnpm install --frozen-lockfile → pnpm build → pnpm start)"
    - "packages/shared/types.ts exists with the convention header (empty; Phase 1+ adds Zod schemas)"
  artifacts:
    - path: "apps/web/package.json"
      provides: "Next.js dependency manifest with pinned versions per RESEARCH.md Standard Stack Web"
      contains: "\"next\": \"^16.2.6\""
    - path: "apps/web/src/app/page.tsx"
      provides: "Async Server Component placeholder page that fetches /health"
      contains: "Personal Resource &amp; Asset Navigator"
    - path: "apps/web/Dockerfile"
      provides: "Multi-stage Docker build for the web container (node:22-alpine)"
      contains: "FROM node:22-alpine"
    - path: "packages/shared/types.ts"
      provides: "TypeScript domain types module (empty in Phase 0; Phase 1+ populates with Zod schemas mirroring packages/shared/schemas.py)"
      contains: "domain types"
  key_links:
    - from: "apps/web/src/app/page.tsx"
      to: "API_URL_INTERNAL or NEXT_PUBLIC_API_URL + /health"
      via: "fetch() in async Server Component"
      pattern: "fetch.*health"
    - from: "apps/web/Dockerfile"
      to: "apps/web/package.json"
      via: "COPY apps/web/package.json + pnpm install --frozen-lockfile"
      pattern: "pnpm install.*frozen-lockfile"

tags:
  - nextjs
  - react
  - typescript
  - tailwind
  - infra
---

<objective>
Stand up the Next.js 16.2.6 web app. The placeholder page is the only feature; everything else is dependency installation, tooling configuration, and Dockerfile scaffolding for the Wave 4 compose stack.

Purpose: CLAUDE.md exit criterion #2 ("http://localhost:3000 shows the PRANAV placeholder with API health status"). Also creates `packages/shared/types.ts` (CLAUDE.md repo structure line 70 — empty with header; Phase 1+ populates). Installs the v1 client-side libraries (TanStack Query, Zustand, RHF, Zod) NOW so versions are locked into `package.json` without a mid-phase bump (RESEARCH.md Supporting (Web) note at line 165).

Output: A bootable Next.js app whose RSC root page fetches the API's /health and renders the result (or "API unavailable" if the fetch fails — CLAUDE.md line 147 "If API is unreachable, show 'API unavailable' gracefully — no crash"). Plus a multi-stage Dockerfile that produces a working production image.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/00-repo-skeleton/00-RESEARCH.md

<interfaces>
<!-- The API /health response shape (from Plan 02), which the web page consumes. -->

GET {API_URL_INTERNAL or NEXT_PUBLIC_API_URL}/health
Response (200):
  {
    "status": "ok" | "degraded",
    "version": "0.1.0",
    "environment": "dev" | "production" | "test",
    "db_connected": true | false
  }
Response (network failure, fetch rejects): handled in page — render "API unavailable" gracefully.

<!-- Tailwind 4 + shadcn/ui idioms (per RESEARCH.md Pitfall 4 and Pitfall 11). -->

Tailwind 4 uses CSS-based config in globals.css (@theme block), NOT a tailwind.config.ts file.
components.json from `shadcn init` for a Tailwind 4 project has `"tailwind": { "config": "" }` (empty string).

<!-- Server Component data fetching (Next.js 16 App Router). -->

In src/app/page.tsx:
  export default async function Page() { ... }
  const res = await fetch(url, { cache: "no-store" });   // RSC server-side fetch
NOTE: Next.js 16 uses async params/cookies/headers — do NOT use the sync versions (CLAUDE.md line 149).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js 16 via create-next-app, install client-side libs, init shadcn/ui</name>
  <files>apps/web/package.json, apps/web/pnpm-lock.yaml, apps/web/tsconfig.json, apps/web/next.config.ts, apps/web/eslint.config.mjs, apps/web/postcss.config.mjs, apps/web/components.json, apps/web/.nvmrc, apps/web/src/app/layout.tsx, apps/web/src/app/globals.css, packages/shared/types.ts</files>

  <read_first>
    - CLAUDE.md (lines 142-149 — Next.js app requirements; line 142 — "Next.js 16.2+, Node 22, TypeScript, Tailwind CSS 4, App Router"; line 143 — "shadcn/ui, TanStack Query v5, React Hook Form, Zod"; line 149 — "Next.js 16 uses async params/cookies()/headers() — never use sync versions"; line 154 — "Do not use JavaScript — TypeScript everywhere")
    - .planning/phases/00-repo-skeleton/00-RESEARCH.md (Standard Stack Core Web at lines 145-159; Supporting Web at lines 161-173; Installation block at lines 218-224; Pitfall 4 at lines 1097-1102; Pitfall 11 at lines 1146-1151)
  </read_first>

  <action>
**Sub-step 1.1 — scaffold the Next.js app.** From the repo root, run:

  `cd /Users/pranavlodha/Documents/ledgr && pnpm create next-app@16.2.6 apps/web --yes --no-git --use-pnpm`

The `--yes` flag accepts ALL defaults per RESEARCH.md Alternatives Considered (line 188): TypeScript + ESLint + Tailwind 4 + App Router + Turbopack + `@/*` alias + `src/` directory + AGENTS/CLAUDE generation. The `--no-git` flag prevents create-next-app from initializing a nested git repo. The `--use-pnpm` forces pnpm as package manager (matches RESEARCH.md §Cheat Sheet "Package manager? pip + requirements.txt (api); pnpm (web)").

This generates:
  - `apps/web/package.json` with `next@^16.2.6`, `react@^19.2.6`, `react-dom@^19.2.6`, `typescript@^5`, `tailwindcss@^4`, `eslint@^9`, `eslint-config-next@^16.2.6`, `@types/node@^25`, `@types/react@^19`
  - `apps/web/tsconfig.json` with paths `@/*` → `src/*`
  - `apps/web/next.config.ts` (TypeScript config; default empty)
  - `apps/web/eslint.config.mjs` (flat config extending `next/core-web-vitals` + `next/typescript`)
  - `apps/web/postcss.config.mjs` (Tailwind 4 via `@tailwindcss/postcss`)
  - `apps/web/src/app/{layout.tsx, page.tsx, globals.css}` (Tailwind-styled defaults)

VERIFY: After scaffold, `cat apps/web/package.json | python3 -c "import json,sys; p=json.load(sys.stdin); assert p['dependencies']['next'].startswith('^16') or p['dependencies']['next'].startswith('16'), p['dependencies']['next']"` succeeds.

**Sub-step 1.2 — install client-side libs (Phase 1+ uses; Phase 0 locks versions).** Run:

  `cd apps/web && pnpm add @tanstack/react-query@^5 zustand@^5 react-hook-form@^7 zod@^4 @hookform/resolvers@^5`

Per RESEARCH.md Supporting (Web) lines 161-170, these are installed but NOT wired in Phase 0. The point is to lock the versions into `package.json` + `pnpm-lock.yaml` now so Phase 1's plan doesn't bump deps.

**Sub-step 1.3 — initialize shadcn/ui (Tailwind 4-compatible).** Run:

  `cd apps/web && pnpm dlx shadcn@latest init --yes`

Per RESEARCH.md §Standard Stack note at lines 173-174, this writes `apps/web/components.json` and updates `globals.css` with CSS variables for theming. For Tailwind 4, the `components.json` will set `"tailwind": { "config": "" }` (empty) and `"cssVariables": true` (per RESEARCH.md Assumption A6 line 1188). If the CLI prompts for `--style`/`--base-color`, the `--yes` flag accepts defaults; if any prompt cannot be auto-answered, accept `default` style and `neutral` base color.

VERIFY after init: `test -f apps/web/components.json` succeeds. Open `components.json` and confirm `"cssVariables": true`. NO `apps/web/tailwind.config.ts` should exist (per RESEARCH.md Pitfall 4 — Tailwind 4 is CSS-config-first).

**Sub-step 1.4 — pin Node version.** Create `apps/web/.nvmrc` containing exactly the single line: `22` (matches `node:22-alpine` in the Dockerfile in Task 2 and Plan 06's CI matrix per RESEARCH.md Pitfall 10).

**Sub-step 1.5 — create packages/shared/types.ts.** This is the TypeScript counterpart to `packages/shared/schemas.py` (Plan 03 created the Python side). The file is empty with a leading JSDoc-style header:

  ```
  /**
   * packages/shared/types.ts — TypeScript domain types shared across the web app.
   *
   * ALL domain types live here (per CLAUDE.md constraint #2).
   * Never define a domain shape inline in a component.
   *
   * Phase 0: empty. Phase 1+ adds:
   *   - HouseholdSchema (Zod) + Household (z.infer type)
   *   - UserSchema (Zod) + User
   *   - AccountSchema, TransactionSchema, etc.
   *
   * Convention:
   *   - Define Zod schemas; derive TS types via z.infer<typeof Schema>.
   *   - Share validation between API requests (TypeScript types) and form validation (React Hook Form + @hookform/resolvers/zod).
   *   - Monetary fields are number (integer cents). Display conversion uses Intl.NumberFormat (per RESEARCH.md §Money + Date Handling).
   *   - Date fields are string (ISO 8601). Convert to Date only at render time.
   */

  export {};
  ```

The `export {};` at the bottom makes the file a module (TypeScript treats files without imports/exports as scripts; `export {}` forces module mode). NO actual type definitions yet — Phase 1+ populates.

**Sub-step 1.6 — verify the scaffold runs.** Run `cd apps/web && pnpm tsc --noEmit` — must exit 0. Run `cd apps/web && pnpm lint` — must exit 0. The default `src/app/page.tsx` shipped by create-next-app passes these checks; Task 2 below will replace `page.tsx` with the PRANAV placeholder.

DO NOT add `next/legacy/image`. DO NOT use `getStaticProps`/`getServerSideProps`. DO NOT create `tailwind.config.ts` (RESEARCH.md Pitfall 4). DO NOT install Recharts (Phase 8). DO NOT install component libraries other than shadcn/ui (RESEARCH.md What NOT to Use line 440 — MUI/Chakra/Mantine).
  </action>

  <verify>
    <automated>test -f apps/web/package.json && python3 -c "import json; p=json.load(open('apps/web/package.json')); assert p['dependencies']['next'].lstrip('^').startswith('16.2'), p['dependencies']['next']; assert '@tanstack/react-query' in p['dependencies']; assert 'zustand' in p['dependencies']; assert 'react-hook-form' in p['dependencies']; assert 'zod' in p['dependencies']; assert '@hookform/resolvers' in p['dependencies']" && test -f apps/web/tsconfig.json && test -f apps/web/next.config.ts && test -f apps/web/eslint.config.mjs && test -f apps/web/postcss.config.mjs && test -f apps/web/components.json && python3 -c "import json; c=json.load(open('apps/web/components.json')); assert c.get('tailwind', {}).get('config', '') == '', c.get('tailwind'); assert c.get('cssVariables') is True, c" && ! test -f apps/web/tailwind.config.ts && test "$(cat apps/web/.nvmrc)" = "22" && test -f packages/shared/types.ts && grep -q 'packages/shared/types.ts' packages/shared/types.ts && grep -q 'CLAUDE.md constraint #2' packages/shared/types.ts && grep -q 'export {};' packages/shared/types.ts</automated>
  </verify>

  <acceptance_criteria>
    - `apps/web/package.json` exists and `dependencies.next` starts with `^16.2` or `16.2` (RESEARCH.md confirmed 16.2.6 latest on 2026-05-23)
    - `apps/web/package.json` `dependencies` contains: `react` (`^19.2`), `react-dom` (`^19.2`), `@tanstack/react-query` (`^5`), `zustand` (`^5`), `react-hook-form` (`^7`), `zod` (`^4`), `@hookform/resolvers` (`^5`)
    - `apps/web/package.json` `devDependencies` contains: `typescript` (`^5`), `tailwindcss` (`^4`), `eslint` (`^9`), `eslint-config-next` (`^16.2`), `@types/node` (`^25`), `@types/react` (`^19`)
    - `apps/web/tsconfig.json` exists with `compilerOptions.paths` mapping `@/*` to `./src/*`
    - `apps/web/next.config.ts` exists (TypeScript next config — NOT next.config.mjs/.js)
    - `apps/web/eslint.config.mjs` exists (flat config — NOT `.eslintrc.json`)
    - `apps/web/postcss.config.mjs` exists referencing `@tailwindcss/postcss`
    - `apps/web/components.json` exists with `"tailwind": { "config": "" }` (empty string for Tailwind 4 per RESEARCH.md Assumption A6) AND `"cssVariables": true`
    - `apps/web/tailwind.config.ts` does NOT exist (Tailwind 4 is CSS-first; per RESEARCH.md Pitfall 4)
    - `apps/web/.nvmrc` contains exactly the line `22`
    - `apps/web/src/app/{layout.tsx, page.tsx, globals.css}` exist (default scaffold from create-next-app)
    - `packages/shared/types.ts` exists with a leading JSDoc-style header mentioning "CLAUDE.md constraint #2", "Phase 0: empty", "Zod schemas", "INTEGER cents" (or similar money note), and ends with `export {};`
    - Running `cd apps/web && pnpm tsc --noEmit` exits 0
    - Running `cd apps/web && pnpm lint` exits 0
    - Running `cd apps/web && pnpm ls --depth=0 next` shows next version starting with 16.2 (not 14.x, not 15.x — per CLAUDE.md line 142 and PROJECT.md decision "Next.js 16.2+ (upgraded from 14)")
  </acceptance_criteria>

  <done>
    apps/web/ is a working Next.js 16.2 project with all v1 client-side dependencies installed and version-locked. shadcn/ui is initialized. packages/shared/types.ts exists as the TypeScript counterpart to packages/shared/schemas.py. The default create-next-app page renders; Task 2 replaces it with the PRANAV placeholder.
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace src/app/page.tsx with PRANAV placeholder (Server Component fetching /health)</name>
  <files>apps/web/src/app/page.tsx, apps/web/src/app/layout.tsx</files>

  <read_first>
    - CLAUDE.md (lines 144-149 — page.tsx requirements; line 147 — "If API is unreachable, show 'API unavailable' gracefully — no crash"; line 149 — async params/cookies/headers in Next 16)
    - .planning/phases/00-repo-skeleton/00-RESEARCH.md (Pattern 11 placeholder page at lines 993-1026; Architectural Responsibility Map "Server-rendered placeholder page" at line 101)
    - apps/web/src/app/layout.tsx (the default create-next-app layout — confirm root metadata before editing)
  </read_first>

  <action>
**Edit `apps/web/src/app/layout.tsx`** — update the metadata title to match the project (CLAUDE.md line 144 — "Personal Resource & Asset Navigator for Abundant Value" heading):

  - Keep the root `<html lang="en">` and `<body className={...}>` structure that create-next-app generated
  - Update the exported `metadata` object's `title` to `"PRANAV — Personal Resource & Asset Navigator for Abundant Value"`
  - Update the `description` to `"Self-hostable personal finance app. See where your money is going before it goes there."`
  - Do NOT remove the Tailwind class on `<body>` (create-next-app sets one like `${inter.variable} antialiased` — preserve it; that's how the Tailwind base layer applies)
  - Do NOT remove the `<html>` `lang` attribute (a11y requirement)

**Replace `apps/web/src/app/page.tsx`** entirely with the placeholder described in RESEARCH.md Pattern 11 (lines 993-1026), adapted to TypeScript-strict and Next.js 16 idioms:

  ```
  // apps/web/src/app/page.tsx
  // Server Component (default in App Router). Async function signature is the Next 16 idiom.

  interface Health {
    status: string;
    version: string;
    environment: string;
    db_connected: boolean;
  }

  async function getHealth(): Promise<Health | null> {
    // In Docker, the web container reaches the api by service name (API_URL_INTERNAL).
    // For local dev outside Docker, fall back to NEXT_PUBLIC_API_URL (set to http://localhost:8000 in .env.example).
    const base = process.env.API_URL_INTERNAL ?? process.env.NEXT_PUBLIC_API_URL ?? "http://localhost:8000";
    const url = `${base}/health`;
    try {
      const res = await fetch(url, { cache: "no-store" });
      if (!res.ok) return null;
      return (await res.json()) as Health;
    } catch {
      return null;
    }
  }

  export default async function Page() {
    const health = await getHealth();
    return (
      <main className="min-h-screen flex items-center justify-center p-8">
        <div className="max-w-md w-full text-center space-y-4">
          <h1 className="text-3xl font-bold">
            Personal Resource &amp; Asset Navigator for Abundant Value
          </h1>
          <p className="text-sm opacity-70">Coming soon.</p>
          <div className="mt-8 p-4 border rounded text-left text-sm font-mono">
            {health ? (
              <pre>{JSON.stringify(health, null, 2)}</pre>
            ) : (
              <span className="opacity-70">API unavailable</span>
            )}
          </div>
        </div>
      </main>
    );
  }
  ```

KEY POINTS:

  - The file MUST start with the `Health` interface definition (or import from `@/types` later) — NOT `any` typing per CLAUDE.md "TypeScript everywhere".
  - The `getHealth` function MUST wrap fetch in try/catch and return `null` on any failure — CLAUDE.md line 147 "no crash".
  - The fetch MUST use `cache: "no-store"` so dev-time changes to the API surface immediately (no stale RSC cache).
  - The base URL resolution order is `API_URL_INTERNAL` (Docker-network name set by docker-compose) THEN `NEXT_PUBLIC_API_URL` (browser-visible URL) THEN `http://localhost:8000` (last-resort hardcoded dev default). This matches RESEARCH.md Pattern 11 line 998.
  - The page is a default-export `async function Page()` — App Router Server Component idiom.
  - NO `"use client"` directive at the top — this is a Server Component, fetching happens server-side.
  - NO sync `cookies()`/`headers()`/`params` — Next 16 made these async (CLAUDE.md line 149). The placeholder doesn't use them anyway.
  - The text in the `<h1>` MUST use HTML entity `&amp;` for the ampersand (per RESEARCH.md Pattern 11 line 1013) so React's JSX parser is happy.

DO NOT add `"use client"`. DO NOT use `useEffect`/`useState`. DO NOT install or import a HTTP client library (`axios`, `ky`) — global `fetch` is fine.

After editing, run `cd apps/web && pnpm tsc --noEmit && pnpm lint` — both must exit 0.
  </action>

  <verify>
    <automated>grep -q 'export default async function Page()' apps/web/src/app/page.tsx && grep -q 'interface Health' apps/web/src/app/page.tsx && grep -q 'async function getHealth(): Promise<Health | null>' apps/web/src/app/page.tsx && grep -q 'API_URL_INTERNAL' apps/web/src/app/page.tsx && grep -q 'NEXT_PUBLIC_API_URL' apps/web/src/app/page.tsx && grep -q 'cache: "no-store"' apps/web/src/app/page.tsx && grep -q 'catch' apps/web/src/app/page.tsx && grep -q 'API unavailable' apps/web/src/app/page.tsx && grep -q 'Personal Resource &amp; Asset Navigator for Abundant Value' apps/web/src/app/page.tsx && ! grep -q '"use client"' apps/web/src/app/page.tsx && ! grep -q 'useEffect' apps/web/src/app/page.tsx && ! grep -q 'useState' apps/web/src/app/page.tsx && grep -q 'PRANAV — Personal Resource' apps/web/src/app/layout.tsx && cd apps/web && pnpm tsc --noEmit && pnpm lint</automated>
  </verify>

  <acceptance_criteria>
    - `apps/web/src/app/page.tsx` defines `interface Health` with exactly four fields: `status: string`, `version: string`, `environment: string`, `db_connected: boolean`
    - `apps/web/src/app/page.tsx` defines `async function getHealth(): Promise<Health | null>` that:
      - Resolves base URL via `process.env.API_URL_INTERNAL ?? process.env.NEXT_PUBLIC_API_URL ?? "http://localhost:8000"`
      - Calls `fetch(url, { cache: "no-store" })`
      - Returns `null` on non-OK response and on any thrown exception (try/catch wraps the entire fetch)
    - `apps/web/src/app/page.tsx` defines `export default async function Page()` that renders:
      - An `<h1>` containing literal substring `Personal Resource &amp; Asset Navigator for Abundant Value` (HTML-escaped ampersand)
      - A `<pre>` containing the JSON-stringified health response when `health !== null`, or a `<span>` containing literal `API unavailable` otherwise
    - `apps/web/src/app/page.tsx` does NOT contain `"use client"` directive (Server Component)
    - `apps/web/src/app/page.tsx` does NOT use `useEffect`, `useState`, or any React hook (Server Component)
    - `apps/web/src/app/page.tsx` does NOT import from `next/headers` or `next/cookies` (no need; would also need to be async-awaited in Next 16)
    - `apps/web/src/app/layout.tsx` metadata `title` updated to a string containing `PRANAV — Personal Resource`
    - `apps/web/src/app/layout.tsx` retains the `<html lang="en">` element (a11y)
    - `cd apps/web && pnpm tsc --noEmit` exits 0
    - `cd apps/web && pnpm lint` exits 0
  </acceptance_criteria>

  <done>
    apps/web/src/app/page.tsx is a Server Component that fetches the API /health on each render (no-store), displays the PRANAV title + the response JSON, and gracefully degrades to "API unavailable" when the API is down. Once Wave 4 wires the docker-compose API_URL_INTERNAL env var, visiting http://localhost:3000 will show the page with the live db_connected status from the api container.
  </done>
</task>

<task type="auto">
  <name>Task 3: Multi-stage web Dockerfile (node:22-alpine)</name>
  <files>apps/web/Dockerfile, apps/web/.dockerignore</files>

  <read_first>
    - CLAUDE.md (line 142 — "Node 22 (node:22-alpine in Dockerfile)"; lines 96-110 — docker-compose requirements)
    - .planning/phases/00-repo-skeleton/00-RESEARCH.md (Pattern 7 Dockerfiles at lines 830-876 verbatim; Web Dockerfile caveat at line 876)
  </read_first>

  <action>
Create `apps/web/Dockerfile` reproducing RESEARCH.md Pattern 7 web Dockerfile (lines 849-873) with these EXACT stages:

  ```
  # apps/web/Dockerfile — multi-stage build for Next.js 16 production runtime.
  # Stage 1 (deps): install dependencies into a deterministic layer.
  # Stage 2 (builder): produce .next build artifacts.
  # Stage 3 (runner): minimal runtime image containing only the runnable bundle.

  FROM node:22-alpine AS deps
  WORKDIR /app
  RUN corepack enable pnpm
  COPY apps/web/package.json apps/web/pnpm-lock.yaml ./
  RUN pnpm install --frozen-lockfile

  FROM node:22-alpine AS builder
  WORKDIR /app
  RUN corepack enable pnpm
  COPY --from=deps /app/node_modules ./node_modules
  COPY apps/web ./
  RUN pnpm build

  FROM node:22-alpine AS runner
  WORKDIR /app
  ENV NODE_ENV=production
  RUN corepack enable pnpm
  COPY --from=builder /app/.next ./.next
  COPY --from=builder /app/public ./public
  COPY --from=builder /app/package.json ./package.json
  COPY --from=builder /app/node_modules ./node_modules
  EXPOSE 3000
  CMD ["pnpm", "start"]
  ```

KEY POINTS:
  - `corepack enable pnpm` is the Node 22 idiom to enable pnpm without a global install. Per RESEARCH.md Environment Availability line 1238.
  - `pnpm install --frozen-lockfile` enforces the committed `pnpm-lock.yaml` — no version drift between dev and image.
  - The `builder` stage runs `pnpm build` which produces `.next/` (Turbopack output).
  - The `runner` stage copies ONLY the runtime artifacts (`.next`, `public`, `package.json`, `node_modules`) — multi-stage shrinks the image (~50% smaller, per RESEARCH.md Alternatives note at line 189).
  - `EXPOSE 3000` matches the docker-compose port mapping in Plan 05.
  - `CMD ["pnpm", "start"]` invokes `next start` (the package.json `start` script from create-next-app).
  - Image base is `node:22-alpine` (matches `.nvmrc`, RESEARCH.md Pitfall 10, CLAUDE.md line 142).

The docker-compose dev mode in Plan 05 will OVERRIDE this `CMD` with `pnpm dev` (Turbopack hot-reload) via bind-mounts; the Dockerfile itself produces a production-style image. This dual behavior is intentional — same Dockerfile works for both dev (bind-mount + dev command) and production (no mount + default CMD).

Create `apps/web/.dockerignore` with these EXACT entries (one per line):

  node_modules
  .next
  .turbo
  .git
  .env
  .env.local
  .env.*.local
  npm-debug.log*
  pnpm-debug.log*
  yarn-debug.log*
  README.md
  .DS_Store
  .vscode
  .idea

This prevents the build context from including `node_modules` (it's regenerated inside the image) and `.env` files (secrets must never enter the image layers).

NOTE: Per RESEARCH.md Web Dockerfile caveat (line 876), `output: "standalone"` in `next.config.ts` would produce an even smaller runtime image (only the strict-runtime files), but it can have subtle issues with the placeholder page. Phase 0 ships the default multi-stage approach above; revisit standalone output at v1.0 ship per BACKLOG.md (Plan 06).
  </action>

  <verify>
    <automated>test -f apps/web/Dockerfile && grep -q 'FROM node:22-alpine AS deps' apps/web/Dockerfile && grep -q 'FROM node:22-alpine AS builder' apps/web/Dockerfile && grep -q 'FROM node:22-alpine AS runner' apps/web/Dockerfile && grep -q 'corepack enable pnpm' apps/web/Dockerfile && grep -q 'pnpm install --frozen-lockfile' apps/web/Dockerfile && grep -q 'pnpm build' apps/web/Dockerfile && grep -q 'EXPOSE 3000' apps/web/Dockerfile && grep -q 'CMD \["pnpm", "start"\]' apps/web/Dockerfile && grep -q 'ENV NODE_ENV=production' apps/web/Dockerfile && grep -q '^node_modules$' apps/web/.dockerignore && grep -q '^.next$' apps/web/.dockerignore && grep -q '^.env$' apps/web/.dockerignore && grep -q '^.git$' apps/web/.dockerignore</automated>
  </verify>

  <acceptance_criteria>
    - `apps/web/Dockerfile` exists with three stages named exactly `deps`, `builder`, `runner` (each `FROM node:22-alpine AS <name>`)
    - `deps` stage runs `corepack enable pnpm`, copies only `package.json` + `pnpm-lock.yaml`, and runs `pnpm install --frozen-lockfile`
    - `builder` stage copies `node_modules` from `deps` stage, copies `apps/web` source, runs `pnpm build`
    - `runner` stage sets `ENV NODE_ENV=production`, copies `.next`, `public`, `package.json`, `node_modules` from `builder` stage
    - `runner` stage `EXPOSE 3000` and `CMD ["pnpm", "start"]`
    - All three stages use the same base image `node:22-alpine` (matches `.nvmrc` 22 from Task 1; matches Plan 05 docker-compose; matches RESEARCH.md Pitfall 10)
    - `apps/web/.dockerignore` exists with entries: `node_modules`, `.next`, `.turbo`, `.git`, `.env`, `.env.local`, `.env.*.local`, `*.debug.log*`, `README.md`, `.DS_Store`, `.vscode`, `.idea`
    - `.dockerignore` does NOT exclude `public/` or `src/` (those must enter the build context)
    - The Dockerfile does NOT install a global `npm install -g pnpm` (use corepack instead per RESEARCH.md Pitfall 11 note + Environment Availability)
    - The Dockerfile does NOT copy `.env` files (secrets stay in env vars at runtime per Plan 05 docker-compose)
  </acceptance_criteria>

  <done>
    apps/web/Dockerfile produces a working Next.js 16 production image. After Plan 05 wires the docker-compose web service to build from this Dockerfile (with bind-mounts overriding for dev hot-reload), `docker compose up --build web` succeeds and the container responds on port 3000.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| web SSR fetch → api | RSC server-side fetches use `API_URL_INTERNAL` (Docker network) — no client-side credential exposure |
| browser → web | Future client-side mutations (Phase 1+) will use `NEXT_PUBLIC_API_URL`; Phase 0 has no client-side fetches |
| Docker build context → image layers | `.dockerignore` prevents `.env`, `node_modules`, `.git` from entering layers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-15 | Information Disclosure | API URL leaked to browser via `NEXT_PUBLIC_*` prefix | accept | Per Next.js convention, `NEXT_PUBLIC_*` vars are intentionally browser-visible. `NEXT_PUBLIC_API_URL=http://localhost:8000` is the public dev value; no secret. Future API auth tokens will NOT use `NEXT_PUBLIC_*` prefix; they'll be set in HttpOnly cookies via the Phase 1 auth flow. |
| T-00-16 | Information Disclosure | `/health` response rendered in HTML — exposes version + environment | accept | Same as T-00-07 (Plan 02): `/health` returns only non-sensitive fields. Acceptable to display verbatim. |
| T-00-17 | Tampering | Supply chain — npm packages (next, react, tailwind, eslint, tanstack-query, zustand, rhf, zod, hookform-resolvers, shadcn deps) | mitigate | All packages are widely-used industry-standard libraries with multi-million weekly download counts (RESEARCH.md §Standard Stack Web + Package Legitimacy Audit note at line 258); `pnpm install --frozen-lockfile` enforces lockfile integrity; image build re-installs deterministically; no transitive package surprise after the lockfile is committed. |
| T-00-18 | Denial of Service | Server-side fetch in RSC blocks page render until API responds | accept | Phase 0 is dev/local; no production load. `cache: "no-store"` was a deliberate choice to surface API state immediately; trade-off accepted. Production would add a fetch timeout (deferred to Phase 1+). |
| T-00-19 | Tampering | Dockerfile builder stage running as root user | accept | Phase 0 ships without an explicit `USER` directive; same convention as the API Dockerfile. Hardening to non-root user is in `docs/BACKLOG.md` (Plan 06) per RESEARCH.md Threat "Container running as root" (line 1332). Acceptable for dev images. |
| T-00-SC | Tampering | npm/pnpm installs of next, react, react-dom, typescript, tailwindcss, eslint, eslint-config-next, @types/*, @tanstack/react-query, zustand, react-hook-form, zod, @hookform/resolvers, plus shadcn transitive deps (clsx, tailwind-merge, class-variance-authority) | mitigate | RESEARCH.md §Package Legitimacy Audit note at line 258 confirms all proposed web packages are well-known with active maintenance and multi-million weekly downloads on npm; no [SUS]/[SLOP] candidates. `pnpm-lock.yaml` is committed; future installs are deterministic via `--frozen-lockfile`. Per planner_authority_limits, no `[ASSUMED]` items requiring human checkpoint. |

</threat_model>

<verification>
- `cd apps/web && pnpm tsc --noEmit && pnpm lint` exits 0 after all three tasks
- `cd apps/web && pnpm build` succeeds (next build with Turbopack)
- `docker build -f apps/web/Dockerfile -t pranav-web:dev .` (from repo root) succeeds and produces an image (runtime verification waits on Wave 4 docker-compose)
- After Wave 4 docker-compose is up: visit http://localhost:3000 → renders the PRANAV heading + the health JSON (assuming api container is up and /health responds)
- If the api container is down: http://localhost:3000 still renders without crashing; the health area shows "API unavailable" (CLAUDE.md line 147)
- `packages/shared/types.ts` exists alongside `packages/shared/schemas.py` — CLAUDE.md repo structure satisfied
</verification>

<success_criteria>
- All grep gates in `<verify>` blocks pass
- Plan 04 ships when `pnpm tsc --noEmit && pnpm lint` exits 0
- Next.js version is 16.2.x (NOT 14.x — per PROJECT.md key decision "Next.js 16.2+ (upgraded from 14)")
- No `tailwind.config.ts` exists (Tailwind 4 is CSS-first)
- The placeholder page degrades gracefully when API is down
</success_criteria>

<output>
Create `.planning/phases/00-repo-skeleton/00-04-SUMMARY.md` when done. Record:
  - Files created (~14 files including scaffold + new + edited)
  - Next.js version installed (confirm `^16.2.x`)
  - shadcn/ui components.json verified (cssVariables=true, tailwind.config="")
  - Web Dockerfile passed `docker build` smoke test (Y/N + size if Y)
  - Any deviations from RESEARCH.md Pattern 7 / Pattern 11 (with reason)
</output>
