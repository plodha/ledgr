---
phase: 00-repo-skeleton
plan: 05
type: execute
wave: 4
depends_on: ["00-01", "00-02", "00-03", "00-04"]
files_modified:
  - apps/api/Dockerfile
  - apps/api/.dockerignore
  - docker/docker-compose.yml
  - Makefile
autonomous: true
requirements:
  - INFRA-01
  - INFRA-03
  - INFRA-04
  - INFRA-05

must_haves:
  truths:
    - "docker/docker-compose.yml defines exactly 4 services: postgres (16-alpine), api, web, adminer (latest)"
    - "Postgres container has a healthcheck (pg_isready); api depends on postgres via condition: service_healthy"
    - "api container runs uvicorn with the apps.api.src.main:app target, mounts apps/api and packages as bind-volumes for dev reload"
    - "web container builds from apps/web/Dockerfile, mounts apps/web with anonymous volumes for node_modules and .next (preserving install)"
    - "adminer container exposes 8080 and connects to the postgres service by name"
    - "Makefile defines targets: dev, down, migrate, check, check-api, check-web, shell-api, shell-db — each invokes docker compose with --env-file .env -f docker/docker-compose.yml"
    - "make shell-db uses POSTGRES_USER=pranav (NOT finbrain — CLAUDE.md legacy patched per RESEARCH.md Assumption A5)"
  artifacts:
    - path: "apps/api/Dockerfile"
      provides: "Multi-stage python:3.12-slim image producing runnable uvicorn container"
      contains: "FROM python:3.12-slim AS builder"
    - path: "docker/docker-compose.yml"
      provides: "4-service compose stack: postgres, api, web, adminer"
      contains: "postgres:16-alpine"
    - path: "Makefile"
      provides: "dev, down, migrate, check, shell-api, shell-db targets"
      contains: ".PHONY: dev down migrate check"
  key_links:
    - from: "docker/docker-compose.yml api service"
      to: "apps/api/Dockerfile + DATABASE_URL env var"
      via: "build.dockerfile + environment block + depends_on postgres"
      pattern: "postgresql\\+asyncpg"
    - from: "docker/docker-compose.yml web service"
      to: "apps/web/Dockerfile + api service"
      via: "depends_on api; env API_URL_INTERNAL=http://api:8000"
      pattern: "API_URL_INTERNAL.*api:8000"
    - from: "Makefile"
      to: "docker/docker-compose.yml"
      via: "COMPOSE := docker compose -f docker/docker-compose.yml --env-file .env"
      pattern: "docker compose.*docker-compose.yml.*--env-file .env"

tags:
  - docker
  - docker-compose
  - makefile
  - infra
  - integration
---

<objective>
Wire all four containers together with docker-compose, finalize the API Dockerfile, and expose the developer interface via Makefile targets. After this plan, `make dev` produces a running 4-container stack that satisfies the bulk of Phase 0 exit criteria.

Purpose: INFRA-01 (`make dev` starts 4 containers), INFRA-03 (`make check` target invocation path), INFRA-04 (`make migrate` runs Alembic inside api container), INFRA-05 (Adminer accessible at localhost:8080).

Output: A working docker-compose stack + a Makefile that orchestrates every dev workflow. Plan 06 will add the documentation, CI workflow, and the end-of-phase human-verify checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/00-repo-skeleton/00-RESEARCH.md
@apps/api/requirements.txt
@apps/api/requirements-dev.txt
@apps/api/src/main.py
@apps/web/Dockerfile
@.env.example

<interfaces>
<!-- Cross-plan contracts that this plan integrates. -->

From Plan 02 (Wave 1):
  - apps/api/src/main.py exports `app` (FastAPI instance)
  - uvicorn target: `apps.api.src.main:app`

From Plan 03 (Wave 2):
  - Alembic config lives at packages/db/alembic.ini
  - `alembic upgrade head` (with cwd=packages/db OR `-c packages/db/alembic.ini`) applies 0001_phase0_baseline

From Plan 04 (Wave 3):
  - apps/web/Dockerfile is a multi-stage node:22-alpine build
  - apps/web/src/app/page.tsx reads API_URL_INTERNAL env var

From Plan 01 (Wave 0):
  - .env.example documents POSTGRES_USER=pranav, POSTGRES_PASSWORD=pranav_dev_password, POSTGRES_DB=pranav, SECRET_KEY, ENVIRONMENT, LOG_LEVEL, ALLOWED_ORIGINS, NEXT_PUBLIC_API_URL

Docker network DNS:
  - Service name `postgres` resolves to the postgres container from `api`
  - Service name `api` resolves to the api container from `web`
  - Browser uses host port mappings: 5432, 8000, 3000, 8080
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: API Dockerfile + .dockerignore</name>
  <files>apps/api/Dockerfile, apps/api/.dockerignore</files>

  <read_first>
    - CLAUDE.md (lines 96-110 — docker-compose requirements; lines 58-60 — apps/api/Dockerfile listed in repo structure)
    - .planning/phases/00-repo-skeleton/00-RESEARCH.md (Pattern 7 API Dockerfile at lines 832-847 verbatim; Pitfall 10 at lines 1139-1144 — Python version pinning)
    - apps/api/requirements.txt (Plan 01 output — runtime deps)
    - apps/api/requirements-dev.txt (Plan 01 output — dev/test deps; installed in builder for `make check` inside container)
  </read_first>

  <action>
Create `apps/api/Dockerfile` reproducing RESEARCH.md Pattern 7 API Dockerfile (lines 832-847) with these EXACT stages:

  ```
  # apps/api/Dockerfile — multi-stage build for FastAPI runtime.
  # Stage 1 (builder): install Python deps into user-site for layer reuse.
  # Stage 2 (runtime): minimal image containing only the runnable bits.

  FROM python:3.12-slim AS builder
  WORKDIR /build
  # Install build-time deps for asyncpg (which sometimes needs gcc) only if a wheel is missing;
  # python:3.12-slim already includes the C runtime libs asyncpg needs at runtime.
  RUN apt-get update && apt-get install -y --no-install-recommends gcc libpq-dev && rm -rf /var/lib/apt/lists/*
  COPY apps/api/requirements.txt apps/api/requirements-dev.txt ./
  # Install BOTH runtime + dev deps in the builder; the runtime stage will copy /root/.local
  # which means `make check` (pytest, ruff, pyright) can run inside the api container.
  RUN pip install --no-cache-dir --user -r requirements.txt -r requirements-dev.txt

  FROM python:3.12-slim AS runtime
  ENV PYTHONUNBUFFERED=1 \
      PYTHONDONTWRITEBYTECODE=1 \
      PATH=/root/.local/bin:$PATH \
      PYTHONPATH=/app
  WORKDIR /app
  # libpq is needed by asyncpg at runtime; install minimal runtime deps only.
  RUN apt-get update && apt-get install -y --no-install-recommends libpq5 && rm -rf /var/lib/apt/lists/*
  COPY --from=builder /root/.local /root/.local
  COPY apps/api /app/apps/api
  COPY packages /app/packages
  EXPOSE 8000
  CMD ["uvicorn", "apps.api.src.main:app", "--host", "0.0.0.0", "--port", "8000"]
  ```

KEY POINTS:
  - Base image `python:3.12-slim` pins Python to 3.12 (matches `.python-version` from Plan 01, CI matrix in Plan 06, per RESEARCH.md Pitfall 10).
  - Builder installs `gcc` + `libpq-dev` for any source-only wheels (asyncpg ships pre-built wheels for cpython 3.12 on Linux, but the dev deps include `greenlet` which might need gcc on some platforms — defensive install).
  - Builder installs BOTH `requirements.txt` AND `requirements-dev.txt` so `make check-api` (pytest + ruff + pyright) can run via `docker compose exec api` (Makefile Task 3 wires this).
  - `pip install --user` lands deps in `/root/.local` — the runtime stage copies only that directory; this halves image size compared to a system-wide install.
  - Runtime stage strips out gcc + dev libs (just keeps `libpq5` shared lib for asyncpg).
  - `PYTHONPATH=/app` lets `uvicorn apps.api.src.main:app` resolve the module from the repo-root layout (the layout is `/app/apps/api/src/main.py` after the COPY); pip is run in /build but the actual code is at /app.
  - `WORKDIR /app` + `COPY apps/api /app/apps/api` + `COPY packages /app/packages` reproduces the repo's top-level structure inside the container.
  - `CMD ["uvicorn", "apps.api.src.main:app", "--host", "0.0.0.0", "--port", "8000"]` matches the docker-compose `command:` override in Task 2 (which adds `--reload` for dev).

Create `apps/api/.dockerignore` with these EXACT entries:

  __pycache__
  *.pyc
  *.pyo
  .pytest_cache
  .ruff_cache
  .pyright
  .venv
  venv
  *.egg-info
  .coverage
  htmlcov
  .git
  .env
  .env.*
  README.md
  docs

This prevents `__pycache__`, virtualenvs, and `.env` from entering image layers (per the security threat model — secrets must NEVER enter image layers).

VERIFY: `docker build -f apps/api/Dockerfile -t pranav-api:dev .` (from repo root, with Docker daemon running) succeeds and produces an image. If `gcc` install fails due to network in a constrained CI environment, document the issue but don't block — the Phase 0 docker-compose runs locally.
  </action>

  <verify>
    <automated>test -f apps/api/Dockerfile && grep -q 'FROM python:3.12-slim AS builder' apps/api/Dockerfile && grep -q 'FROM python:3.12-slim AS runtime' apps/api/Dockerfile && grep -q 'pip install --no-cache-dir --user -r requirements.txt -r requirements-dev.txt' apps/api/Dockerfile && grep -q 'PYTHONUNBUFFERED=1' apps/api/Dockerfile && grep -q 'PYTHONDONTWRITEBYTECODE=1' apps/api/Dockerfile && grep -q 'PATH=/root/.local/bin' apps/api/Dockerfile && grep -q 'COPY --from=builder /root/.local /root/.local' apps/api/Dockerfile && grep -q 'COPY apps/api /app/apps/api' apps/api/Dockerfile && grep -q 'COPY packages /app/packages' apps/api/Dockerfile && grep -q 'EXPOSE 8000' apps/api/Dockerfile && grep -q 'CMD \["uvicorn", "apps.api.src.main:app"' apps/api/Dockerfile && grep -q '^__pycache__$' apps/api/.dockerignore && grep -q '^\.env$' apps/api/.dockerignore && grep -q '^\.git$' apps/api/.dockerignore && grep -q '^\.venv$' apps/api/.dockerignore</automated>
  </verify>

  <acceptance_criteria>
    - `apps/api/Dockerfile` exists with two stages named `builder` and `runtime` (each `FROM python:3.12-slim AS <name>`)
    - `builder` stage runs `apt-get install -y --no-install-recommends gcc libpq-dev` (for any source-builds)
    - `builder` stage copies BOTH `apps/api/requirements.txt` AND `apps/api/requirements-dev.txt` and runs `pip install --no-cache-dir --user -r requirements.txt -r requirements-dev.txt`
    - `runtime` stage sets env vars `PYTHONUNBUFFERED=1`, `PYTHONDONTWRITEBYTECODE=1`, `PATH=/root/.local/bin:$PATH`, `PYTHONPATH=/app`
    - `runtime` stage installs `libpq5` (asyncpg runtime dep) but NOT `gcc`/`libpq-dev` (those are builder-only)
    - `runtime` stage copies `/root/.local` from builder + `apps/api` from build context + `packages` from build context
    - `runtime` stage `EXPOSE 8000` and `CMD ["uvicorn", "apps.api.src.main:app", "--host", "0.0.0.0", "--port", "8000"]`
    - `apps/api/.dockerignore` contains: `__pycache__`, `*.pyc`, `.pytest_cache`, `.ruff_cache`, `.venv`, `venv`, `.git`, `.env`, `.env.*`
    - `.dockerignore` does NOT exclude `src/`, `tests/`, `requirements.txt`, `requirements-dev.txt`, `pyproject.toml` (those must enter the build context)
    - Image produced by `docker build -f apps/api/Dockerfile -t pranav-api:dev .` from repo root contains `pytest`, `ruff`, `pyright` in `/root/.local/bin` (verifiable by `docker run --rm pranav-api:dev which pytest` — output is non-empty)
  </acceptance_criteria>

  <done>
    apps/api/Dockerfile produces a working FastAPI runtime image with all dev/test tooling included. The image can run `uvicorn apps.api.src.main:app` (default CMD) AND `pytest`/`ruff`/`pyright`/`alembic` via `docker compose exec api ...`. Task 2 wires docker-compose to use this Dockerfile.
  </done>
</task>

<task type="auto">
  <name>Task 2: docker/docker-compose.yml (4-service stack with healthcheck + bind-mounts)</name>
  <files>docker/docker-compose.yml</files>

  <read_first>
    - CLAUDE.md (lines 96-110 — docker-compose requirements; line 101 — postgres:16-alpine port 5432 volume; line 102-104 — api builds from apps/api/Dockerfile port 8000 depends on postgres; line 105-107 — web port 3000 depends on api; line 108 — adminer port 8080; line 109 — restart: unless-stopped)
    - .planning/phases/00-repo-skeleton/00-RESEARCH.md (Pattern 6 docker-compose at lines 758-828 verbatim; Pitfall 2 at lines 1083-1088 — Postgres healthcheck required; Anti-Patterns line 1038 — no `version:` key)
    - apps/api/Dockerfile (Task 1 — build target reference)
    - apps/web/Dockerfile (Plan 04 — build target reference)
    - .env.example (Plan 01 — variable names for ${VAR:-default} substitution)
  </read_first>

  <action>
Create `docker/docker-compose.yml` reproducing RESEARCH.md Pattern 6 (lines 758-828) with these EXACT services. NO `version:` key at the top (Compose v2 deprecated it; RESEARCH.md Anti-Pattern line 1038):

  ```
  # docker/docker-compose.yml
  # PRANAV Phase 0 dev stack: postgres + api + web + adminer.
  # All services restart: unless-stopped per CLAUDE.md.
  # Run from repo root via: make dev (which sets --env-file .env).

  services:
    postgres:
      image: postgres:16-alpine
      restart: unless-stopped
      environment:
        POSTGRES_USER: ${POSTGRES_USER:-pranav}
        POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-pranav_dev_password}
        POSTGRES_DB: ${POSTGRES_DB:-pranav}
      ports:
        - "5432:5432"
      volumes:
        - postgres_data:/var/lib/postgresql/data
      healthcheck:
        test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-pranav} -d ${POSTGRES_DB:-pranav}"]
        interval: 5s
        timeout: 5s
        retries: 5

    api:
      build:
        context: ..
        dockerfile: apps/api/Dockerfile
      restart: unless-stopped
      depends_on:
        postgres:
          condition: service_healthy
      environment:
        DATABASE_URL: postgresql+asyncpg://${POSTGRES_USER:-pranav}:${POSTGRES_PASSWORD:-pranav_dev_password}@postgres:5432/${POSTGRES_DB:-pranav}
        SECRET_KEY: ${SECRET_KEY:-change-me-in-prod}
        ENVIRONMENT: ${ENVIRONMENT:-dev}
        LOG_LEVEL: ${LOG_LEVEL:-INFO}
        ALLOWED_ORIGINS: ${ALLOWED_ORIGINS:-http://localhost:3000}
      ports:
        - "8000:8000"
      volumes:
        - ../apps/api:/app/apps/api
        - ../packages:/app/packages
      command: ["uvicorn", "apps.api.src.main:app", "--host", "0.0.0.0", "--port", "8000", "--reload"]

    web:
      build:
        context: ..
        dockerfile: apps/web/Dockerfile
      restart: unless-stopped
      depends_on:
        - api
      environment:
        NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL:-http://localhost:8000}
        API_URL_INTERNAL: http://api:8000
      ports:
        - "3000:3000"
      volumes:
        - ../apps/web:/app
        - /app/node_modules
        - /app/.next

    adminer:
      image: adminer:latest
      restart: unless-stopped
      depends_on:
        - postgres
      ports:
        - "8080:8080"
      environment:
        ADMINER_DEFAULT_SERVER: postgres

  volumes:
    postgres_data:
  ```

KEY POINTS:
  - `build.context: ..` is relative to `docker/` — points up to repo root so Dockerfiles can `COPY apps/api` etc. (CRITICAL: `make dev` from repo root with `-f docker/docker-compose.yml` requires this).
  - `depends_on.postgres.condition: service_healthy` waits for the Postgres healthcheck to pass before starting api (RESEARCH.md Pitfall 2 — without this, `make migrate` may run before Postgres is ready).
  - api's `DATABASE_URL` uses `postgresql+asyncpg://` driver prefix (RESEARCH.md Pitfall 1).
  - api's volumes mount BOTH `apps/api` AND `packages` (the latter contains `packages/db/migrations`, needed for `make migrate`).
  - api's `command:` adds `--reload` to uvicorn (overrides the production CMD in the Dockerfile for dev hot-reload).
  - web's volume strategy: bind-mount source (`../apps/web:/app`) PLUS anonymous volumes for `/app/node_modules` and `/app/.next` (this preserves the install/build done inside the image, even when the bind-mount shadows /app's contents — standard Next.js + Docker dev pattern).
  - web's `API_URL_INTERNAL=http://api:8000` uses the Docker service name `api` (NOT `localhost`) — Server Components fetch from inside the Docker network.
  - web's `NEXT_PUBLIC_API_URL=http://localhost:8000` uses host port (NOT service name) — would be needed if any future client-side fetch leaks; Phase 0 only uses API_URL_INTERNAL.
  - adminer's `ADMINER_DEFAULT_SERVER=postgres` pre-fills the server field in the Adminer login form (UX nicety — RESEARCH.md INFRA-05 says "no auth on the dev port").
  - Top-level `volumes: postgres_data:` declares the named volume (Postgres data persists across `docker compose down`).
  - NO `version:` key at the top (RESEARCH.md Anti-Pattern line 1038 — deprecated in Compose v2).
  - NO Redis service (RESEARCH.md What NOT to Use line 429 — Postgres handles jobs via procrastinate in Phase 4).
  - NO procrastinate-worker service (RESEARCH.md Pitfall 6 — deferred to Phase 4).
  - NO Sentry/OpenTelemetry collector (RESEARCH.md Deferred Ideas line 73).

VERIFY: `docker compose -f docker/docker-compose.yml --env-file .env config` (with `.env` created from `.env.example`) must validate the YAML and resolve all variable interpolations. This is the same command the Makefile uses; failures here are blocking.
  </action>

  <verify>
    <automated>test -f docker/docker-compose.yml && ! grep -q '^version:' docker/docker-compose.yml && grep -q 'postgres:16-alpine' docker/docker-compose.yml && grep -q 'pg_isready' docker/docker-compose.yml && grep -q 'condition: service_healthy' docker/docker-compose.yml && grep -q 'postgresql+asyncpg' docker/docker-compose.yml && grep -q 'DATABASE_URL: postgresql+asyncpg://${POSTGRES_USER' docker/docker-compose.yml && grep -q 'dockerfile: apps/api/Dockerfile' docker/docker-compose.yml && grep -q 'dockerfile: apps/web/Dockerfile' docker/docker-compose.yml && grep -q 'image: adminer:latest' docker/docker-compose.yml && grep -q 'API_URL_INTERNAL: http://api:8000' docker/docker-compose.yml && grep -q '"8000:8000"' docker/docker-compose.yml && grep -q '"3000:3000"' docker/docker-compose.yml && grep -q '"8080:8080"' docker/docker-compose.yml && grep -q '"5432:5432"' docker/docker-compose.yml && grep -c 'restart: unless-stopped' docker/docker-compose.yml | grep -q '^4$' && grep -q '\-\-reload' docker/docker-compose.yml && ! grep -q 'redis' docker/docker-compose.yml && ! grep -q 'procrastinate' docker/docker-compose.yml && ! grep -q 'sentry' docker/docker-compose.yml && grep -q '^volumes:' docker/docker-compose.yml && grep -q '  postgres_data:' docker/docker-compose.yml</automated>
  </verify>

  <acceptance_criteria>
    - `docker/docker-compose.yml` does NOT contain a top-level `version:` key (Compose v2; RESEARCH.md Anti-Pattern line 1038)
    - File defines exactly 4 services: `postgres`, `api`, `web`, `adminer`
    - All 4 services have `restart: unless-stopped` (CLAUDE.md line 109; grep count = 4)
    - `postgres` uses image `postgres:16-alpine`, exposes port `5432:5432`, mounts volume `postgres_data:/var/lib/postgresql/data`, has healthcheck running `pg_isready` (RESEARCH.md Pitfall 2)
    - `postgres.environment` sets `POSTGRES_USER`, `POSTGRES_PASSWORD`, `POSTGRES_DB` from env with defaults `pranav`, `pranav_dev_password`, `pranav`
    - `api.build.context: ..` and `api.build.dockerfile: apps/api/Dockerfile`
    - `api.depends_on.postgres.condition: service_healthy` (waits for Postgres healthcheck)
    - `api.environment.DATABASE_URL` uses `postgresql+asyncpg://` driver prefix (RESEARCH.md Pitfall 1) and interpolates POSTGRES_* env vars + Docker service name `postgres`
    - `api.environment` includes `SECRET_KEY`, `ENVIRONMENT`, `LOG_LEVEL`, `ALLOWED_ORIGINS` (matches Settings fields from Plan 02)
    - `api.volumes` mounts BOTH `../apps/api:/app/apps/api` AND `../packages:/app/packages` (latter is required for `alembic upgrade head` to find `packages/db/migrations`)
    - `api.command` includes `--reload` flag (dev hot-reload)
    - `api.ports` exposes `8000:8000`
    - `web.build.context: ..` and `web.build.dockerfile: apps/web/Dockerfile`
    - `web.depends_on: [api]`
    - `web.environment.API_URL_INTERNAL: http://api:8000` (Docker service name, NOT `localhost`)
    - `web.environment.NEXT_PUBLIC_API_URL` interpolated from env with default `http://localhost:8000`
    - `web.ports` exposes `3000:3000`
    - `web.volumes` bind-mounts `../apps/web:/app` AND declares anonymous volumes `/app/node_modules` AND `/app/.next` (Next.js + Docker dev pattern)
    - `adminer.image: adminer:latest`, `adminer.depends_on: [postgres]`, `adminer.ports: "8080:8080"`, `adminer.environment.ADMINER_DEFAULT_SERVER: postgres`
    - Top-level `volumes: postgres_data:` declared (named volume for Postgres data persistence)
    - File does NOT define `redis`, `procrastinate-worker`, `sentry`, or any other service (deferred per RESEARCH.md)
    - `docker compose -f docker/docker-compose.yml --env-file .env config` (with a `.env` copied from `.env.example`) validates and resolves all variables without error
  </acceptance_criteria>

  <done>
    docker/docker-compose.yml is a valid 4-service Compose v2 stack. Running `docker compose -f docker/docker-compose.yml --env-file .env up --build` from repo root brings up all 4 containers. Postgres health-gates the api startup. api can reach postgres by service name; web can reach api by service name. Volumes preserve Postgres data + Next.js node_modules.
  </done>
</task>

<task type="auto">
  <name>Task 3: Makefile (dev, down, migrate, check, shell-api, shell-db)</name>
  <files>Makefile</files>

  <read_first>
    - CLAUDE.md (lines 113-120 — Makefile targets — KEY NOTE: line 120 says `make shell-db → docker compose exec postgres psql -U finbrain`; the username `finbrain` is LEGACY and MUST be patched to `pranav` per RESEARCH.md Assumption A5 (line 1187) and Open Question 6 (lines 1226-1229))
    - .planning/phases/00-repo-skeleton/00-RESEARCH.md (Pattern 8 Makefile at lines 878-908 verbatim)
    - docker/docker-compose.yml (Task 2 output — referenced by every Makefile target)
  </read_first>

  <action>
Create `Makefile` at repo root reproducing RESEARCH.md Pattern 8 (lines 880-908). The KEY DEVIATION from CLAUDE.md line 120: use `pranav` not `finbrain` for the `make shell-db` username (per RESEARCH.md Assumption A5 / Open Question 6 — the project renamed; CLAUDE.md's example was not updated; this plan patches the convention).

EXACT contents:

  ```
  # Makefile — PRANAV Phase 0 developer interface.
  # Run from repo root. All targets wrap docker compose with the Phase 0 compose file + .env.
  #
  # Quickstart:
  #   cp .env.example .env
  #   make dev       # boots 4 containers
  #   make migrate   # (in a second terminal) applies Alembic baseline
  #   make check     # runs lints + typechecks + tests (host-side)
  #
  # NOTE: `make shell-db` uses POSTGRES_USER=pranav by default. This patches the legacy
  # `finbrain` example in CLAUDE.md per RESEARCH.md Assumption A5 (project renamed 2026-05-23).

  .PHONY: dev down migrate check check-api check-web shell-api shell-db help

  # Default target: print help
  .DEFAULT_GOAL := help

  COMPOSE := docker compose -f docker/docker-compose.yml --env-file .env

  help:
  	@echo "PRANAV Makefile targets:"
  	@echo "  make dev          - Build and start all 4 containers (postgres, api, web, adminer)"
  	@echo "  make down         - Stop and remove containers (postgres_data volume persists)"
  	@echo "  make migrate      - Run Alembic upgrade head inside the api container"
  	@echo "  make check        - Run all lints + typechecks + tests (api + web)"
  	@echo "  make check-api    - Run only api checks (ruff + pyright + pytest)"
  	@echo "  make check-web    - Run only web checks (tsc --noEmit + eslint)"
  	@echo "  make shell-api    - Open a bash shell in the api container"
  	@echo "  make shell-db     - Open a psql session against the postgres container (user: pranav)"

  dev:
  	$(COMPOSE) up --build

  down:
  	$(COMPOSE) down

  migrate:
  	$(COMPOSE) exec api alembic -c packages/db/alembic.ini upgrade head

  check: check-api check-web

  check-api:
  	cd apps/api && ruff check . && ruff format --check . && pyright && pytest

  check-web:
  	cd apps/web && pnpm tsc --noEmit && pnpm lint

  shell-api:
  	$(COMPOSE) exec api bash

  shell-db:
  	$(COMPOSE) exec postgres psql -U $${POSTGRES_USER:-pranav} -d $${POSTGRES_DB:-pranav}
  ```

KEY POINTS:
  - The `COMPOSE := ...` macro centralizes the compose invocation; every target reuses it.
  - `--env-file .env` is REQUIRED — without it, `${POSTGRES_USER:-pranav}` etc. in docker-compose.yml don't get resolved from the developer's local `.env`.
  - `make migrate` uses `docker compose exec api alembic -c packages/db/alembic.ini upgrade head` — the `-c` flag points Alembic at the config file in the mounted `packages/` volume. CWD is `/app` inside the container; the path resolves correctly.
  - `make check` runs HOST-SIDE (NOT inside the container) per RESEARCH.md Pattern 8 (line 898). This means the developer must `pip install -r apps/api/requirements-dev.txt` on the host first, OR run `make shell-api` and execute `ruff check . && pyright && pytest` from inside the container. The host-side approach is faster for the inner dev loop; the container-side option is documented in README (Plan 06).
  - `make shell-db` uses `pranav` not `finbrain` (PATCH from CLAUDE.md line 120 per RESEARCH.md A5/O6).
  - `$${POSTGRES_USER:-pranav}` in `shell-db` — the double-dollar `$$` escapes Make's variable expansion so the shell sees `${POSTGRES_USER:-pranav}` and uses its OWN env (which docker compose exec inherits from the host env, which is sourced from `.env` via `make dev`'s `--env-file` is NOT in scope for `exec`; the `${...:-pranav}` shell fallback handles the case).
  - Tabs (NOT spaces) for indented recipe lines — Makefile syntax is strict.
  - `.PHONY:` declares all non-file targets to prevent collision with files of the same name.
  - `.DEFAULT_GOAL := help` makes bare `make` print the usage instead of failing.

VERIFY: `make help` prints the usage block (smoke test that the Makefile parses).
  </action>

  <verify>
    <automated>test -f Makefile && grep -q '^\.PHONY: dev down migrate check' Makefile && grep -q '^COMPOSE := docker compose -f docker/docker-compose.yml --env-file .env' Makefile && grep -q '^dev:' Makefile && grep -q '^down:' Makefile && grep -q '^migrate:' Makefile && grep -q '^check:' Makefile && grep -q '^check-api:' Makefile && grep -q '^check-web:' Makefile && grep -q '^shell-api:' Makefile && grep -q '^shell-db:' Makefile && grep -q '\$(COMPOSE) up --build' Makefile && grep -q '\$(COMPOSE) down' Makefile && grep -q 'alembic -c packages/db/alembic.ini upgrade head' Makefile && grep -q 'cd apps/api && ruff check' Makefile && grep -q 'cd apps/web && pnpm tsc --noEmit && pnpm lint' Makefile && grep -q 'psql -U' Makefile && grep -q ':-pranav}' Makefile && ! grep -q 'finbrain' Makefile && make help | grep -q 'PRANAV Makefile targets'</automated>
  </verify>

  <acceptance_criteria>
    - `Makefile` exists at repo root
    - File declares `.PHONY: dev down migrate check check-api check-web shell-api shell-db help` (all non-file targets)
    - File sets `.DEFAULT_GOAL := help` so bare `make` prints usage
    - File defines `COMPOSE := docker compose -f docker/docker-compose.yml --env-file .env` as a make variable
    - Target `dev:` invokes `$(COMPOSE) up --build`
    - Target `down:` invokes `$(COMPOSE) down`
    - Target `migrate:` invokes `$(COMPOSE) exec api alembic -c packages/db/alembic.ini upgrade head`
    - Target `check:` depends on `check-api check-web` (composite)
    - Target `check-api:` runs `cd apps/api && ruff check . && ruff format --check . && pyright && pytest`
    - Target `check-web:` runs `cd apps/web && pnpm tsc --noEmit && pnpm lint`
    - Target `shell-api:` invokes `$(COMPOSE) exec api bash`
    - Target `shell-db:` invokes `$(COMPOSE) exec postgres psql -U $${POSTGRES_USER:-pranav} -d $${POSTGRES_DB:-pranav}` — USERNAME defaults to `pranav` NOT `finbrain` (RESEARCH.md A5/O6 patches CLAUDE.md legacy)
    - File contains NO occurrence of the string `finbrain` (legacy name purged)
    - Running `make help` (which is the default goal) prints a block containing the literal substring `PRANAV Makefile targets:`
    - Recipe lines use tab indentation (NOT spaces — Makefile syntax requires tabs; `cat -A Makefile | grep -E "^\^I" | wc -l` returns > 0)
  </acceptance_criteria>

  <done>
    Makefile is functional. `make help` prints usage. After `cp .env.example .env`, running `make dev` brings up the 4-container stack; `make migrate` applies the Alembic baseline; `make check` runs all lints + tests. The `finbrain` legacy username from CLAUDE.md is patched to `pranav` per planning_constraints + RESEARCH.md A5/O6.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host docker engine ↔ container | Containers inherit env from `.env` via `--env-file`; secrets are file-system-readable on the host but not committed |
| api container ↔ postgres container | Communication over Docker bridge network using DSN `postgres://...@postgres:5432/...`; no TLS in dev |
| web container ↔ api container | Server-side fetch over Docker bridge using `http://api:8000`; no TLS in dev |
| browser ↔ host ports (5432, 8000, 3000, 8080) | Localhost-only in dev; never exposed to public networks without explicit reverse proxy |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-20 | Spoofing | Adminer's dev-only no-auth posture on port 8080 | accept (dev) | Phase 0 is dev-only on localhost. Adminer's default behavior is "anyone who can reach the port can log into Postgres" — acceptable when bound to localhost only. Production deployment (out of scope for Phase 0) will not expose port 8080. Documented in README (Plan 06). |
| T-00-21 | Information Disclosure | `.env` file containing real SECRET_KEY/PASSWORD readable on dev machine | mitigate (Phase 0) / defer (production) | `.gitignore` (Plan 01) ensures `.env` never lands in git. File-system permission hardening is dev-machine responsibility; not Phase 0 scope. Production deployment will use Docker secrets/Kubernetes secrets/Vault per phase 12+ deployment work. |
| T-00-22 | Tampering | Docker image base images `python:3.12-slim`, `node:22-alpine`, `postgres:16-alpine`, `adminer:latest` from public Docker Hub | accept | All images are official, widely-used base images. No digest pinning in Phase 0 (RESEARCH.md V14 Configuration line 1321 explicitly accepts this risk for Phase 0). Pinning by digest is in `docs/BACKLOG.md` (Plan 06). |
| T-00-23 | Elevation of Privilege | Containers run as root (no USER directive in either Dockerfile) | accept (Phase 0) | Both Dockerfiles ship without `USER` directive; same convention as RESEARCH.md Threat Pattern "Container running as root" (line 1332) and T-00-19 (Plan 04). Hardening to non-root is in BACKLOG.md. |
| T-00-24 | Denial of Service | api container's `--reload` flag causing memory growth on rapid file edits | accept (dev) | uvicorn `--reload` is the standard dev mode; trade-off is known. Production CMD (Dockerfile default, no `--reload`) is used when this command-override is not in effect. |
| T-00-25 | Information Disclosure | Postgres `pg_isready` healthcheck running `pg_isready -U pranav` (username in command line visible via `ps`) | accept | Username is not a secret; password is in `POSTGRES_PASSWORD` env var, never on command line. Standard convention. |
| T-00-26 | Tampering | docker-compose.yml mounting `../packages:/app/packages` allows host-side edits to packages/ to flow into the container | accept (dev) | This is the desired behavior — dev hot-reload requires bind mounts. Production deployment will not use bind mounts; production image bakes the code in at build time (see api Dockerfile Task 1). |

</threat_model>

<verification>
- `docker compose -f docker/docker-compose.yml --env-file .env config` validates and prints the resolved configuration
- `make help` prints the usage block
- After Wave 5 (Plan 06) creates docs/README.md instructing `cp .env.example .env && make dev`: running those commands brings up 4 healthy containers (`docker compose ps` shows postgres health=healthy, api/web/adminer state=running)
- `make migrate` (after `make dev` is up and postgres is healthy) prints `Running upgrade -> 0001_phase0_baseline` from Alembic
- `make check-api` (with host-side deps installed per Plan 06 README) runs ruff + pyright + pytest; pytest may have lingering failures until ALL waves complete (e.g., conftest.py needs to import from src.main which Plan 02 created — should be green by now)
- `make shell-db` opens a psql prompt connected as `pranav` to database `pranav`
- INFRA-01, INFRA-04, INFRA-05 are NOW satisfied at the orchestration layer; final runtime verification is the end-of-phase human-verify checkpoint in Plan 06
</verification>

<success_criteria>
- All grep gates in `<verify>` blocks pass
- Plan 05 ships when:
  - `docker compose -f docker/docker-compose.yml --env-file .env config` validates (after a `.env` is copied from `.env.example`)
  - `make help` prints usage
- `docker compose ps` shows 4 services UP when `make dev` runs in a separate terminal (this verification happens in Plan 06's human-verify checkpoint after the doc + CI work is in)
- No `finbrain` string anywhere in Makefile or docker-compose.yml (legacy purged per RESEARCH.md A5)
</success_criteria>

<output>
Create `.planning/phases/00-repo-skeleton/00-05-SUMMARY.md` when done. Record:
  - Files created (4 files)
  - Result of `docker compose -f docker/docker-compose.yml --env-file .env config` validation
  - Result of `docker build -f apps/api/Dockerfile -t pranav-api:dev .` smoke test (success + image size)
  - Confirmation that `finbrain` legacy username is purged (grep returns nothing)
  - Confirmation that `version:` key is NOT present in docker-compose.yml (Compose v2)
  - Any deviations from RESEARCH.md Pattern 6/7/8 (with reason)
</output>
