---
phase: 00-repo-skeleton
plan: 03
type: execute
wave: 2
depends_on: ["00-02"]
files_modified:
  - packages/db/__init__.py
  - packages/db/alembic.ini
  - packages/db/models/__init__.py
  - packages/db/migrations/env.py
  - packages/db/migrations/script.py.mako
  - packages/db/migrations/versions/0001_phase0_baseline.py
  - packages/shared/__init__.py
  - packages/shared/schemas.py
  - packages/domain/__init__.py
autonomous: true
requirements:
  - INFRA-04
  - INFRA-09

must_haves:
  truths:
    - "alembic upgrade head succeeds against Postgres 16 using the asyncpg driver"
    - "After upgrade, _phase0_marker table exists with exactly one row whose note starts with 'Phase 0 migration succeeded'"
    - "The app.current_household_id RLS GUC convention is reserved (Phase 1 will attach per-table policies; this phase ONLY reserves the convention)"
    - "packages/db/models/__init__.py exports a DeclarativeBase only — no domain tables yet"
    - "packages/shared/schemas.py exists empty with docstring; packages/domain/__init__.py exists empty with docstring (cross-module call sites for future phases)"
  artifacts:
    - path: "packages/db/alembic.ini"
      provides: "Alembic configuration; script_location resolves to migrations/; sqlalchemy.url overridable via DATABASE_URL env"
      contains: "script_location = migrations"
    - path: "packages/db/migrations/env.py"
      provides: "Async env.py using async_engine_from_config + run_async_migrations()"
      contains: "async_engine_from_config"
    - path: "packages/db/migrations/versions/0001_phase0_baseline.py"
      provides: "Creates _phase0_marker singleton table + reserves RLS GUC convention via COMMENT ON DATABASE"
      contains: "_phase0_marker"
    - path: "packages/db/models/__init__.py"
      provides: "Base = DeclarativeBase only (Phase 1 will add User/Household models)"
      contains: "DeclarativeBase"
    - path: "packages/shared/schemas.py"
      provides: "Empty Pydantic schemas module with docstring (CLAUDE.md mandates all domain types live here)"
      contains: "Pydantic"
    - path: "packages/domain/__init__.py"
      provides: "Empty domain package (CLAUDE.md mandates cross-module calls go through packages/domain/)"
      contains: "domain"
  key_links:
    - from: "packages/db/migrations/env.py"
      to: "DATABASE_URL env var"
      via: "os.getenv('DATABASE_URL') overrides sqlalchemy.url at runtime"
      pattern: "os.getenv\\(.DATABASE_URL.\\)"
    - from: "packages/db/migrations/versions/0001_phase0_baseline.py"
      to: "Postgres database"
      via: "op.execute(CREATE TABLE _phase0_marker ...)"
      pattern: "CREATE TABLE _phase0_marker"

tags:
  - postgres
  - alembic
  - migrations
  - rls
  - infra
---

<objective>
Stand up Alembic with an async env.py and ship the Phase 0 baseline migration. This unblocks `make migrate` (Wave 4 wires the target) and turns Plan 01's `test_baseline_migration_applied` green by creating the `_phase0_marker` table the test queries.

Purpose: INFRA-04 (`make migrate` runs Alembic) and INFRA-09 (Postgres RLS infrastructure scaffolded — `app.current_household_id` GUC convention reserved, NO policies yet per RESEARCH.md Open Question 1 recommendation interpretation (a)). Also creates the empty `packages/shared/schemas.py` and `packages/domain/__init__.py` per CLAUDE.md repo structure (these are NOT throwaways — Phase 1+ depends on them existing).

Per RESEARCH.md Open Question 1 (lines 1198-1205), the user did not provide CONTEXT.md, so this plan uses the researcher's recommended default: **reserve the GUC convention via marker table + COMMENT ON DATABASE; do NOT enable RLS on any specific table in Phase 0** (Postgres has no "cluster-level RLS enable" — RLS is per-table; Phase 1 will `ALTER TABLE ... ENABLE ROW LEVEL SECURITY` per domain table as they are created).

Output: A migration that runs successfully against Postgres 16, a marker table that proves migration succeeded, and the foundation files (`packages/db/models/__init__.py` with `Base`; `packages/shared/schemas.py`; `packages/domain/__init__.py`) that Phase 1+ will extend.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/00-repo-skeleton/00-RESEARCH.md
@apps/api/src/database.py
@apps/api/tests/test_health.py

<interfaces>
<!-- Contract: Plan 01's test_baseline_migration_applied issues this query and asserts the result. -->

After `alembic upgrade head`:
  SELECT note FROM _phase0_marker WHERE id = 1
  → returns exactly one row
  → note text starts with "Phase 0 migration succeeded"

Phase 1 will use the following SQL convention (RESERVED by this plan, NOT activated):
  SET LOCAL app.current_household_id = '<household-uuid>'      -- in get_session() after auth resolves
  CREATE POLICY ... USING (household_id = current_setting('app.current_household_id', true)::uuid)
                                                              ^^^^^^^^^^^^^^^^^^^^
                                                              true (missing_ok) is non-negotiable per RESEARCH.md Pitfall 9

packages/db/models/__init__.py exports (Phase 0):
  from sqlalchemy.orm import DeclarativeBase
  class Base(DeclarativeBase): pass

Phase 1+ models will inherit `Base` and Alembic's autogenerate will diff against `Base.metadata`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: packages/db scaffold — alembic.ini, env.py, script.py.mako, models/__init__.py</name>
  <files>packages/db/__init__.py, packages/db/alembic.ini, packages/db/models/__init__.py, packages/db/migrations/env.py, packages/db/migrations/script.py.mako</files>

  <read_first>
    - CLAUDE.md (lines 61-72 — packages/db structure; line 35 — "make migrate → run alembic upgrade head inside api container")
    - .planning/phases/00-repo-skeleton/00-RESEARCH.md (Pattern 4 Alembic env.py at lines 596-715; Pitfall 1 at lines 1076-1082; Pitfall 2 at lines 1083-1088)
  </read_first>

  <action>
Create `packages/db/__init__.py` as an empty file (with module docstring: `"""packages/db — SQLAlchemy models, Base declarative, and Alembic migrations. Phase 0 reserves the RLS GUC convention; Phase 1+ adds domain tables."""`).

Create `packages/db/models/__init__.py` containing ONLY the declarative base. Exact contents:

  - `from __future__ import annotations`
  - `"""Module docstring — Base declarative model. No domain tables in Phase 0. Phase 1 adds User, Household, etc."""`
  - `from sqlalchemy.orm import DeclarativeBase`
  - `class Base(DeclarativeBase): pass`

NO `__all__` (single export is implicit). NO `Mapped`/`mapped_column` imports yet. NO table classes — that violates CLAUDE.md "Do not create any auth, user, or account tables yet — those are Phase 1".

Create `packages/db/alembic.ini` with these EXACT settings (Alembic 1.18 standard config trimmed to what Phase 0 needs):

  [alembic]
  script_location = migrations
  prepend_sys_path = ../..
  # sqlalchemy.url is overridden by DATABASE_URL env var in env.py (RESEARCH.md Pattern 4 lines 619-622)
  sqlalchemy.url = postgresql+asyncpg://pranav:pranav_dev_password@localhost:5432/pranav

  [post_write_hooks]

  [loggers]
  keys = root,sqlalchemy,alembic

  [handlers]
  keys = console

  [formatters]
  keys = generic

  [logger_root]
  level = WARNING
  handlers = console
  qualname =

  [logger_sqlalchemy]
  level = WARNING
  handlers =
  qualname = sqlalchemy.engine

  [logger_alembic]
  level = INFO
  handlers =
  qualname = alembic

  [handler_console]
  class = StreamHandler
  args = (sys.stderr,)
  level = NOTSET
  formatter = generic

  [formatter_generic]
  format = %(levelname)-5.5s [%(name)s] %(message)s
  datefmt = %H:%M:%S

The `sqlalchemy.url` line is a fallback for direct `alembic` invocation; in normal use the env.py reads `DATABASE_URL` from the environment and overrides this (per RESEARCH.md Pattern 4 lines 619-622, addressing Pitfall 1).

Create `packages/db/migrations/env.py` reproducing RESEARCH.md Pattern 4 (lines 605-652) with these EXACT semantics:

  - `from __future__ import annotations`
  - Imports: `asyncio`, `os`, `from logging.config import fileConfig`, `from alembic import context`, `from sqlalchemy import pool`, `from sqlalchemy.engine import Connection`, `from sqlalchemy.ext.asyncio import async_engine_from_config`
  - `config = context.config`
  - `db_url = os.getenv("DATABASE_URL")` then if truthy: `config.set_main_option("sqlalchemy.url", db_url)` (overrides alembic.ini at runtime — addresses Pitfall 1)
  - `if config.config_file_name is not None: fileConfig(config.config_file_name)`
  - `target_metadata = None` (Phase 0 has no models; Phase 1 changes this to `from packages.db.models import Base; target_metadata = Base.metadata`)
  - Function `def do_run_migrations(connection: Connection) -> None:` that calls `context.configure(connection=connection, target_metadata=target_metadata)` then `with context.begin_transaction(): context.run_migrations()`
  - `async def run_async_migrations() -> None:` that builds `connectable = async_engine_from_config(config.get_section(config.config_ini_section, {}), prefix="sqlalchemy.", poolclass=pool.NullPool)`, then `async with connectable.connect() as connection: await connection.run_sync(do_run_migrations)`, then `await connectable.dispose()`
  - `def run_migrations_online() -> None: asyncio.run(run_async_migrations())`
  - Bottom: `if context.is_offline_mode(): raise NotImplementedError("Offline mode not supported in Phase 0; use online mode")` else `run_migrations_online()`

`NullPool` is required: Alembic runs one-shot from CLI and does not want connection pool state hanging around (RESEARCH.md Pattern 4 line 640).

Create `packages/db/migrations/script.py.mako` — copy the Alembic 1.18 default mako template (`alembic init -t async` generates this; reproduce verbatim). Content:

```
"""${message}

Revision ID: ${up_revision}
Revises: ${down_revision | comma,n}
Create Date: ${create_date}

"""
from __future__ import annotations
from typing import Sequence, Union

from alembic import op
import sqlalchemy as sa
${imports if imports else ""}

# revision identifiers, used by Alembic.
revision: str = ${repr(up_revision)}
down_revision: Union[str, None] = ${repr(down_revision)}
branch_labels: Union[str, Sequence[str], None] = ${repr(branch_labels)}
depends_on: Union[str, Sequence[str], None] = ${repr(depends_on)}


def upgrade() -> None:
    ${upgrades if upgrades else "pass"}


def downgrade() -> None:
    ${downgrades if downgrades else "pass"}
```

Create `packages/db/migrations/versions/` as an empty directory placeholder (the actual revision file lands in Task 2). To create an empty directory in git, add a `.gitkeep` file inside: `packages/db/migrations/versions/.gitkeep` (empty file).
  </action>

  <verify>
    <automated>test -f packages/db/__init__.py && grep -q 'class Base(DeclarativeBase)' packages/db/models/__init__.py && grep -q 'from sqlalchemy.orm import DeclarativeBase' packages/db/models/__init__.py && ! grep -q 'class User' packages/db/models/__init__.py && ! grep -q 'class Household' packages/db/models/__init__.py && grep -q 'script_location = migrations' packages/db/alembic.ini && grep -q 'sqlalchemy.url = postgresql+asyncpg' packages/db/alembic.ini && grep -q 'prepend_sys_path = ../..' packages/db/alembic.ini && grep -q 'async_engine_from_config' packages/db/migrations/env.py && grep -q 'os.getenv("DATABASE_URL")' packages/db/migrations/env.py && grep -q 'NullPool' packages/db/migrations/env.py && grep -q 'target_metadata = None' packages/db/migrations/env.py && grep -q 'async def run_async_migrations' packages/db/migrations/env.py && grep -q 'def do_run_migrations' packages/db/migrations/env.py && grep -q 'asyncio.run(run_async_migrations())' packages/db/migrations/env.py && test -f packages/db/migrations/script.py.mako && grep -q 'revision: str' packages/db/migrations/script.py.mako && test -f packages/db/migrations/versions/.gitkeep</automated>
  </verify>

  <acceptance_criteria>
    - `packages/db/__init__.py` exists with a module docstring (one-liner acceptable)
    - `packages/db/models/__init__.py` exists; imports `DeclarativeBase` from `sqlalchemy.orm`; defines `class Base(DeclarativeBase): pass`
    - `packages/db/models/__init__.py` contains NO class definitions other than `Base` (no User, Household, Account, etc. — Phase 1+ only)
    - `packages/db/alembic.ini` exists; `[alembic]` section sets `script_location = migrations` and `prepend_sys_path = ../..`
    - `packages/db/alembic.ini` `sqlalchemy.url` uses `postgresql+asyncpg://` driver prefix (NOT bare `postgresql://` — RESEARCH.md Pitfall 1)
    - `packages/db/alembic.ini` has `[loggers]`, `[handlers]`, `[formatters]` sections (Alembic requires them or fileConfig fails)
    - `packages/db/migrations/env.py` imports `async_engine_from_config` from `sqlalchemy.ext.asyncio`
    - `packages/db/migrations/env.py` reads `DATABASE_URL` from env via `os.getenv("DATABASE_URL")` and calls `config.set_main_option("sqlalchemy.url", db_url)` when truthy
    - `packages/db/migrations/env.py` sets `target_metadata = None` (Phase 0 — no models to autogenerate against)
    - `packages/db/migrations/env.py` uses `pool.NullPool` for the Alembic engine
    - `packages/db/migrations/env.py` raises `NotImplementedError` in offline mode (Phase 0 is online-only)
    - `packages/db/migrations/script.py.mako` exists and contains `revision: str = ${repr(up_revision)}` and `down_revision: Union[str, None] = ${repr(down_revision)}`
    - `packages/db/migrations/versions/` directory exists (created via `.gitkeep` placeholder)
  </acceptance_criteria>

  <done>
    Running `cd packages/db && alembic current` (inside the api container in Wave 4) succeeds and prints "(no current revision)" — proving env.py loads, connects to Postgres, and reads the alembic_version table (which Alembic creates on first run).
  </done>
</task>

<task type="auto">
  <name>Task 2: Phase 0 baseline migration (0001_phase0_baseline.py)</name>
  <files>packages/db/migrations/versions/0001_phase0_baseline.py</files>

  <read_first>
    - .planning/phases/00-repo-skeleton/00-RESEARCH.md (Pattern 4 baseline migration at lines 656-715; Open Question 1 at lines 1198-1205 — RLS interpretation; Pitfall 9 at lines 1132-1138 — current_setting missing_ok arg; INFRA-09 description at line 90)
    - apps/api/tests/test_health.py (Plan 01 — encodes the contract: `SELECT note FROM _phase0_marker WHERE id = 1` returns a row starting with "Phase 0 migration succeeded")
  </read_first>

  <action>
Create `packages/db/migrations/versions/0001_phase0_baseline.py` reproducing RESEARCH.md Pattern 4 baseline (lines 656-715) with these EXACT semantics:

Module docstring explaining the two purposes (verbatim from RESEARCH.md lines 657-679):

  1. Create `_phase0_marker` — single-row sentinel table proving `make migrate` actually ran. Phase 1 will drop this when real tables arrive.
  2. Reserve the `app.current_household_id` session GUC convention via `COMMENT ON DATABASE`. Postgres custom GUCs with dotted names (`app.*`) need NO registration since Postgres 9.2 (verified via web search 2026-05-23 per RESEARCH.md). The migration documents the convention via SQL comment so future contributors see it. Phase 1 will (a) ENABLE ROW LEVEL SECURITY on each domain table as created, (b) attach policies like `USING (household_id = current_setting('app.current_household_id', true)::uuid)`, (c) have FastAPI's `get_session` issue `SET LOCAL app.current_household_id = '<uuid>'` after auth resolves the household.

Migration body:

  - `from __future__ import annotations`
  - `from alembic import op`
  - Revision identifiers:
      - `revision: str = "0001_phase0_baseline"`
      - `down_revision: str | None = None`
      - `branch_labels: str | None = None`
      - `depends_on: str | None = None`
  - Helper `def _current_db() -> str:` that gets the bind from `context.get_bind()`, reads `bind.engine.url.database`, and returns it as a quoted SQL identifier (handle double-quote escaping)
  - `def upgrade() -> None:`:
      - `op.execute("""<<CREATE TABLE _phase0_marker (id SMALLINT PRIMARY KEY DEFAULT 1, applied_at TIMESTAMPTZ NOT NULL DEFAULT NOW(), note TEXT NOT NULL DEFAULT 'Phase 0 migration succeeded. RLS scaffolded; policies wired in Phase 1.', CONSTRAINT _phase0_marker_singleton CHECK (id = 1));>>""")` — exactly the CREATE TABLE from RESEARCH.md Pattern 4 lines 688-694
      - `op.execute("INSERT INTO _phase0_marker (id) VALUES (1);")` — inserts the singleton row so the test query in Plan 01 returns a row
      - Build the COMMENT statement: `comment = f"COMMENT ON DATABASE {_current_db()} IS 'PRANAV: RLS GUC convention = app.current_household_id (set via SET LOCAL per session; read via current_setting(\\'app.current_household_id\\', true)). Policies attached in Phase 1.';"` and `op.execute(comment)`
        Note the `, true` inside `current_setting(...)` — non-negotiable per RESEARCH.md Pitfall 9 (without it, an unset GUC raises `unrecognized configuration parameter`).
  - `def downgrade() -> None:`:
      - `op.execute("DROP TABLE IF EXISTS _phase0_marker")`
      - Do NOT undo the COMMENT (it's documentation, not state, per RESEARCH.md Pattern 4 line 702)

The migration filename `0001_phase0_baseline.py` matches the `revision` string `"0001_phase0_baseline"` — Alembic uses the revision identifier, not the filename, for chaining; the filename is a human convenience.

The DEFAULT `note` value text must START with the literal string `"Phase 0 migration succeeded"` (Plan 01's `test_baseline_migration_applied` asserts `note.startswith("Phase 0 migration succeeded")`). Use exactly: `'Phase 0 migration succeeded. RLS scaffolded; policies wired in Phase 1.'`

DO NOT enable RLS on `_phase0_marker` or any other table (RESEARCH.md Open Question 1 recommendation (a) — reserve the GUC convention only; Phase 1 attaches policies to real domain tables). DO NOT create domain tables (User, Household, etc.) — those are Phase 1.

Use type annotation `str | None` (Python 3.12 PEP 604 syntax) for the revision identifier slots. The Alembic mako template uses `Union[str, None]`; either works under Python 3.12 with `from __future__ import annotations`.
  </action>

  <verify>
    <automated>test -f packages/db/migrations/versions/0001_phase0_baseline.py && grep -q 'revision: str = "0001_phase0_baseline"' packages/db/migrations/versions/0001_phase0_baseline.py && grep -q 'down_revision: str | None = None' packages/db/migrations/versions/0001_phase0_baseline.py && grep -q 'CREATE TABLE _phase0_marker' packages/db/migrations/versions/0001_phase0_baseline.py && grep -q 'SMALLINT PRIMARY KEY DEFAULT 1' packages/db/migrations/versions/0001_phase0_baseline.py && grep -q 'CONSTRAINT _phase0_marker_singleton CHECK (id = 1)' packages/db/migrations/versions/0001_phase0_baseline.py && grep -q "INSERT INTO _phase0_marker" packages/db/migrations/versions/0001_phase0_baseline.py && grep -q 'COMMENT ON DATABASE' packages/db/migrations/versions/0001_phase0_baseline.py && grep -q "app.current_household_id" packages/db/migrations/versions/0001_phase0_baseline.py && grep -q "current_setting" packages/db/migrations/versions/0001_phase0_baseline.py && grep -q "Phase 0 migration succeeded" packages/db/migrations/versions/0001_phase0_baseline.py && grep -q "def upgrade()" packages/db/migrations/versions/0001_phase0_baseline.py && grep -q "def downgrade()" packages/db/migrations/versions/0001_phase0_baseline.py && grep -q "DROP TABLE IF EXISTS _phase0_marker" packages/db/migrations/versions/0001_phase0_baseline.py && ! grep -q "ENABLE ROW LEVEL SECURITY" packages/db/migrations/versions/0001_phase0_baseline.py && ! grep -q "CREATE POLICY" packages/db/migrations/versions/0001_phase0_baseline.py</automated>
  </verify>

  <acceptance_criteria>
    - `packages/db/migrations/versions/0001_phase0_baseline.py` exists
    - File defines `revision: str = "0001_phase0_baseline"` and `down_revision: str | None = None` (this is the first migration)
    - `upgrade()` executes a `CREATE TABLE _phase0_marker` with: id `SMALLINT PRIMARY KEY DEFAULT 1`, applied_at `TIMESTAMPTZ NOT NULL DEFAULT NOW()`, note `TEXT NOT NULL DEFAULT 'Phase 0 migration succeeded. RLS scaffolded; policies wired in Phase 1.'`, and `CONSTRAINT _phase0_marker_singleton CHECK (id = 1)` (singleton enforcement)
    - `upgrade()` executes an `INSERT INTO _phase0_marker (id) VALUES (1)` after the CREATE TABLE
    - `upgrade()` executes a `COMMENT ON DATABASE` statement whose body contains the literal substring `app.current_household_id` AND the literal substring `current_setting` AND the missing_ok flag `, true)` (per RESEARCH.md Pitfall 9 — non-negotiable)
    - `upgrade()` does NOT execute `ENABLE ROW LEVEL SECURITY` on any table (RESEARCH.md Open Question 1 recommendation (a) — reserve convention only; Phase 1 activates per-table)
    - `upgrade()` does NOT execute `CREATE POLICY` (Phase 1 only)
    - `downgrade()` executes `DROP TABLE IF EXISTS _phase0_marker` and does NOT attempt to undo the COMMENT (per RESEARCH.md Pattern 4 line 702)
    - After this migration runs against a Postgres database, querying `SELECT note FROM _phase0_marker WHERE id = 1` returns a row where `note` text starts with `Phase 0 migration succeeded` (Plan 01 test contract)
    - After this migration runs, querying `SELECT pg_catalog.shobj_description(d.oid, 'pg_database') FROM pg_database d WHERE d.datname = current_database()` returns a description string containing `app.current_household_id`
  </acceptance_criteria>

  <done>
    The migration file is in place. After Wave 4 (docker-compose) starts Postgres and `make migrate` runs the upgrade, the `_phase0_marker` table exists with one row, and the database COMMENT documents the RLS GUC convention. Plan 01's `test_baseline_migration_applied` will pass.
  </done>
</task>

<task type="auto">
  <name>Task 3: packages/shared + packages/domain scaffold (empty modules with docstrings)</name>
  <files>packages/shared/__init__.py, packages/shared/schemas.py, packages/domain/__init__.py</files>

  <read_first>
    - CLAUDE.md (lines 14-20 — domain types live in packages/shared; lines 29-30 — cross-module calls through packages/domain/; lines 61-72 — repo structure with packages/shared and packages/domain)
    - .planning/phases/00-repo-skeleton/00-RESEARCH.md (Recommended Project Structure at lines 360-365 — packages/shared/{__init__.py, schemas.py, types.ts}, packages/domain/__init__.py)
  </read_first>

  <action>
Create `packages/shared/__init__.py` as an empty file with a one-line module docstring:

  `"""packages/shared — domain types shared across api and (via packages/shared/types.ts) web. Phase 0: empty. Phase 1+ populates Pydantic schemas and matching Zod schemas."""`

Create `packages/shared/schemas.py` as an empty Python module with a multi-line docstring stating the contract (per CLAUDE.md constraint #2):

  ```
  """packages/shared/schemas.py — Pydantic v2 base schemas.

  ALL domain types live here (per CLAUDE.md constraint #2).
  Never define a domain shape inline in a route or component.

  Phase 0: empty. Phase 1+ adds:
    - HouseholdCreate, HouseholdRead
    - UserCreate, UserRead, UserLogin
    - AccountCreate, AccountRead
    - TransactionCreate, TransactionRead (with INTEGER cents per CLAUDE.md constraint #4)

  Convention:
    - Use Pydantic v2 BaseModel (not dataclasses, not SQLModel).
    - Read schemas separate from write schemas (e.g., UserCreate vs UserRead).
    - All monetary fields are int (cents). Display conversion lives client-side.
    - All dates are ISO 8601 strings on the wire; DATE/TIMESTAMPTZ in DB.
  """
  ```

The file body after the docstring is empty. NO imports yet, NO class definitions yet (Phase 1 adds these). The docstring itself is the load-bearing artifact — it documents the convention for future contributors.

Create `packages/domain/__init__.py` as an empty Python module with a multi-line docstring stating the contract (per CLAUDE.md constraint #6):

  ```
  """packages/domain — Pure business logic. No DB or HTTP imports.

  Cross-module calls between api modules (apps/api/src/modules/<name>/) MUST
  flow through this package (per CLAUDE.md constraint #6). A module may NOT
  import from another module's internal files.

  Phase 0: empty. Future phases populate:
    - packages/domain/money/      (cents <-> display; Phase 3)
    - packages/domain/dates/      (transaction_date vs post_date; Phase 3)
    - packages/domain/categorization/  (pure rule evaluator; Phase 5 — zero DB calls)
    - packages/domain/transactions/totals.py  (canonical list_transactions_for_totals; Phase 3)
    - packages/domain/forecast/   (rolling 90-day projection; Phase 8)
    - packages/domain/connectors/ (abstract Connector interface; Phase 9)

  Convention:
    - Pure functions where possible. Side effects belong in api modules.
    - No SQLAlchemy imports; no FastAPI imports.
    - Tested without a database (CLAUDE.md "domain layer tests are mandatory").
  """
  ```

The file body after the docstring is empty. NO module-level code yet. This docstring documents the boundary policy that import-linter (Phase 1+) will enforce.

NOTE: `packages/shared/types.ts` is NOT created by this plan — it is created in Plan 04 (Wave 3) alongside the Next.js scaffold, because it's a TypeScript file that the web app's tsconfig will resolve.
  </action>

  <verify>
    <automated>test -f packages/shared/__init__.py && test -f packages/shared/schemas.py && test -f packages/domain/__init__.py && grep -q 'packages/shared' packages/shared/__init__.py && grep -q 'Pydantic' packages/shared/schemas.py && grep -q 'INTEGER cents' packages/shared/schemas.py && grep -q 'TIMESTAMPTZ' packages/shared/schemas.py && grep -q 'Pure business logic' packages/domain/__init__.py && grep -q 'No SQLAlchemy imports' packages/domain/__init__.py && grep -q 'No DB or HTTP' packages/domain/__init__.py</automated>
  </verify>

  <acceptance_criteria>
    - `packages/shared/__init__.py` exists with a module docstring mentioning "packages/shared"
    - `packages/shared/schemas.py` exists with a module docstring that mentions:
        - "Pydantic" (the chosen validation library)
        - "INTEGER cents" (money convention from CLAUDE.md #4)
        - "TIMESTAMPTZ" or "DATE" (date convention from CLAUDE.md #5)
        - "Phase 0: empty" (clarifying current state)
    - `packages/shared/schemas.py` body after the docstring contains NO class definitions and NO imports (Phase 1+ populates)
    - `packages/domain/__init__.py` exists with a multi-line docstring that mentions:
        - "Pure business logic" (the boundary contract)
        - "No SQLAlchemy imports" (the boundary enforcement)
        - "No DB or HTTP" (per CLAUDE.md #6)
        - The future planned subpackages (money, dates, categorization, transactions, forecast, connectors)
    - `packages/domain/__init__.py` body after the docstring contains NO function or class definitions
    - `packages/shared/types.ts` is NOT created in this plan (deferred to Plan 04 — Next.js scaffold)
    - All three files type-check trivially under pyright (no code, no errors)
  </acceptance_criteria>

  <done>
    The `packages/shared/` and `packages/domain/` namespaces exist with documented contracts. Phase 1's first model addition can `from packages.shared.schemas import ...` (the file already exists). Phase 1's first cross-module call can `from packages.domain.<x> import ...`. CLAUDE.md's repo structure (lines 61-72) is satisfied for Phase 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration code → live database | Alembic migrations execute DDL; any error here is permanent without a downgrade |
| developer-laptop psql client → Postgres dev instance | Direct DB access bypasses RLS even when policies exist (BYPASSRLS for superuser); Phase 0 has no policies so this is a non-issue, but documenting for Phase 1 |
| Postgres custom GUC `app.current_household_id` | Phase 1's RLS policies will trust whatever value is bound; the API must SET LOCAL it correctly after auth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-11 | Tampering | Migration injecting unintended SQL via the `_current_db()` helper's database-name interpolation | mitigate | `_current_db()` reads `bind.engine.url.database` (a fixed value from the env-supplied DATABASE_URL, not user input) and wraps it in double-quoted identifier syntax with double-quote escaping. No untrusted input reaches the SQL string. The risk surface is zero in practice. |
| T-00-12 | Information Disclosure | RLS GUC convention reserved without policies — if Phase 1 forgets `current_setting(..., true)`, queries fail with `unrecognized configuration parameter` instead of returning empty | mitigate | RESEARCH.md Pitfall 9 (lines 1132-1138) documents the convention; this migration's COMMENT ON DATABASE includes the `, true` flag inline; docs/MIGRATIONS.md (Plan 06) will re-document the rule. Phase 1's RLS policy migrations will be reviewed against this convention. |
| T-00-13 | Elevation of Privilege | A future Phase 1 forgets to `ALTER TABLE ... ENABLE ROW LEVEL SECURITY` on a new domain table → cross-household data leak | accept (Phase 0) / mitigate (Phase 1) | Phase 0 cannot prevent a Phase 1 oversight. Mitigation lives in Phase 1: (a) `_phase0_marker` proves migration tooling works so Phase 1 can confidently add ENABLE RLS to every domain table; (b) Phase 1's two-household pytest fixture (per ROADMAP success criterion 4) is the integration-level guard; (c) the COMMENT ON DATABASE message + docs/MIGRATIONS.md serves as the standing reminder. Threat is Phase 1's responsibility. |
| T-00-14 | Denial of Service | Migration on a non-empty Phase-1+ database holding a write lock | accept (Phase 0) / document (INFRA-08) | Phase 0 runs against an empty database — no lock contention possible. The non-locking discipline (nullable-first, CREATE INDEX CONCURRENTLY) is documented in docs/MIGRATIONS.md by Plan 06 to set Phase 1+ practice. INFRA-08 explicitly tracks this. |

</threat_model>

<verification>
- `cd packages/db && alembic upgrade head` (run inside the api container after Wave 4) prints `Running upgrade  -> 0001_phase0_baseline`
- After upgrade: `psql -U pranav -d pranav -c "SELECT note FROM _phase0_marker WHERE id = 1"` returns one row with `note` starting `Phase 0 migration succeeded`
- After upgrade: `psql -U pranav -d pranav -c "SELECT pg_catalog.shobj_description(d.oid, 'pg_database') FROM pg_database d WHERE d.datname = 'pranav'"` returns a string containing `app.current_household_id`
- `alembic downgrade base` drops the `_phase0_marker` table cleanly; re-running `alembic upgrade head` restores it (round-trip safe)
- `pytest apps/api/tests/test_health.py::test_baseline_migration_applied` (run inside the api container after `make migrate`) passes
- No RLS policy exists on any table (verified by `SELECT * FROM pg_policy` returning zero rows on a fresh Phase-0 database — Phase 1 will populate)
</verification>

<success_criteria>
- All grep gates in `<verify>` blocks pass
- The 0001 migration file is the SOLE migration; `ls packages/db/migrations/versions/*.py` returns exactly one file
- The migration uses ONLY async-safe Alembic primitives (`op.execute` with raw SQL is fine; no synchronous bulk inserts via SQLAlchemy ORM)
- INFRA-04 and INFRA-09 are now satisfied; runtime verification waits on Wave 4 (`make migrate` target + docker-compose)
</success_criteria>

<output>
Create `.planning/phases/00-repo-skeleton/00-03-SUMMARY.md` when done. Record:
  - Files created (9 files)
  - Confirmation that the Alembic env.py reads DATABASE_URL from env (Pitfall 1 mitigated)
  - Confirmation that the migration's COMMENT ON DATABASE includes `current_setting('app.current_household_id', true)` with the missing_ok flag (Pitfall 9 mitigated)
  - Confirmation that NO `ENABLE ROW LEVEL SECURITY` or `CREATE POLICY` statements appear in Phase 0 (RESEARCH.md Open Question 1 — recommendation (a) honored)
  - Confirmation that `packages/db/models/__init__.py` exports only `Base` (no domain tables)
  - Any deviations from RESEARCH.md Pattern 4 (with reason)
</output>
