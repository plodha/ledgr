---
phase: 00-repo-skeleton
plan: 02
type: execute
wave: 1
depends_on: ["00-01"]
files_modified:
  - apps/api/src/__init__.py
  - apps/api/src/config.py
  - apps/api/src/database.py
  - apps/api/src/logging_config.py
  - apps/api/src/main.py
  - apps/api/src/modules/__init__.py
  - apps/api/src/modules/health/__init__.py
  - apps/api/src/modules/health/router.py
autonomous: true
requirements:
  - INFRA-02
  - INFRA-07

must_haves:
  truths:
    - "FastAPI app starts via uvicorn with title='PRANAV API' and lifespan-managed async DB engine"
    - "Settings load DATABASE_URL, SECRET_KEY, ENVIRONMENT, LOG_LEVEL, ALLOWED_ORIGINS, APP_VERSION via pydantic-settings"
    - "GET /health returns {status, version, environment, db_connected} where db_connected reflects an actual SELECT 1 against Postgres"
    - "structlog emits JSON in production environment, console in dev; password/token/secret/authorization keys are redacted at any nesting depth"
    - "RequestContextMiddleware is pure ASGI (not BaseHTTPMiddleware) and binds request_id to structlog contextvars for every HTTP request"
  artifacts:
    - path: "apps/api/src/config.py"
      provides: "Settings class (pydantic-settings) + get_settings() lru_cached factory"
      contains: "class Settings(BaseSettings)"
    - path: "apps/api/src/database.py"
      provides: "create_async_engine + async_sessionmaker + get_session FastAPI dependency"
      contains: "AsyncSessionLocal"
    - path: "apps/api/src/logging_config.py"
      provides: "configure_logging + RequestContextMiddleware + _redact_processor"
      contains: "class RequestContextMiddleware"
    - path: "apps/api/src/main.py"
      provides: "FastAPI app + lifespan + CORSMiddleware + RequestContextMiddleware + health router include"
      contains: "FastAPI(title=\"PRANAV API\""
    - path: "apps/api/src/modules/health/router.py"
      provides: "APIRouter exposing GET / that returns health JSON with db_connected from SELECT 1"
      contains: "SELECT 1"
  key_links:
    - from: "apps/api/src/main.py"
      to: "apps/api/src/modules/health/router.py"
      via: "app.include_router(health_router, prefix=\"/health\")"
      pattern: "include_router.*health"
    - from: "apps/api/src/modules/health/router.py"
      to: "apps/api/src/database.py:get_session"
      via: "Depends(get_session) injects AsyncSession"
      pattern: "Depends\\(get_session\\)"
    - from: "apps/api/src/main.py"
      to: "apps/api/src/logging_config.py"
      via: "add_middleware(RequestContextMiddleware) + configure_logging()"
      pattern: "add_middleware\\(RequestContextMiddleware\\)"

tags:
  - python
  - fastapi
  - structlog
  - infra
---

<objective>
Implement the FastAPI application that turns Plan 01's failing tests green. This is the load-bearing Phase 0 code: every later phase builds on these patterns (async engine + session, structlog + ASGI middleware, pydantic-settings config, module router include).

Purpose: Satisfy INFRA-02 (`/health` returns `{status, version, environment, db_connected}` from a real SELECT 1) and INFRA-07 (structured JSON logging via structlog with request_id binding and sensitive-field redaction). The exact code shapes are pre-specified verbatim in RESEARCH.md Patterns 1, 2, 3 — copy them rather than reinventing.

Output: Working FastAPI app (`apps/api/src/`) that boots under uvicorn, connects to Postgres, and answers `/health`. After Wave 2 ships the Alembic baseline migration, `pytest apps/api/tests/test_health.py` will pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@CLAUDE.md
@.planning/phases/00-repo-skeleton/00-RESEARCH.md
@apps/api/tests/conftest.py
@apps/api/tests/test_health.py
@apps/api/tests/test_logging.py

<interfaces>
<!-- These are the contracts THIS PLAN must satisfy. They were defined by the test files in Plan 01. -->

From apps/api/tests/conftest.py:
  - `from src.main import app` MUST succeed
  - `app` MUST be a FastAPI instance that supports lifespan startup/shutdown

From apps/api/tests/test_health.py::test_health_ok:
  - `GET /health` → 200
  - response body keys: `status` (str, "ok"), `version` (str), `environment` (str), `db_connected` (bool, True when DB up)

From apps/api/tests/test_health.py::test_baseline_migration_applied:
  - `from src.database import AsyncSessionLocal` MUST succeed
  - `AsyncSessionLocal()` MUST produce an async session that can `await session.execute(text("SELECT ..."))` against Postgres

From apps/api/tests/test_logging.py::test_request_id_bound_via_middleware:
  - `from src.logging_config import RequestContextMiddleware, configure_logging` MUST succeed
  - `RequestContextMiddleware(app).__call__(scope, receive, send)` MUST bind `request_id` into structlog contextvars when scope has `x-request-id` header
  - When the header is absent, request_id MUST default to a uuid4 hex
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Settings + Database modules (apps/api/src/config.py, database.py, __init__.py + module __init__s)</name>
  <files>apps/api/src/__init__.py, apps/api/src/config.py, apps/api/src/database.py, apps/api/src/modules/__init__.py, apps/api/src/modules/health/__init__.py</files>

  <read_first>
    - CLAUDE.md (lines 122-140 — FastAPI requirements, env var list)
    - .planning/phases/00-repo-skeleton/00-RESEARCH.md (Pattern 1 at lines 389-449 — Settings + database.py verbatim; Anti-Pattern bullet "expire_on_commit=True" at line 1033)
    - apps/api/tests/conftest.py (Plan 01 output — imports from src.main and src.database; contract)
  </read_first>

  <action>
Create `apps/api/src/__init__.py` as an empty file (marks `src` as a Python package; `pyproject.toml` lists `src` in `[tool.ruff].src` and `[tool.pyright].include`).

Create `apps/api/src/modules/__init__.py` and `apps/api/src/modules/health/__init__.py` as empty files.

Create `apps/api/src/config.py` reproducing RESEARCH.md Pattern 1 (lines 393-416) with these EXACT semantics:

  - `from __future__ import annotations` at top
  - Import `from functools import lru_cache`
  - Import `from typing import Literal`
  - Import `from pydantic_settings import BaseSettings, SettingsConfigDict`
  - Class `Settings(BaseSettings)` with:
      - `model_config = SettingsConfigDict(env_file=".env", env_file_encoding="utf-8", extra="ignore")`
      - Field `DATABASE_URL: str` (no default — must come from env)
      - Field `SECRET_KEY: str` (no default — must come from env)
      - Field `ENVIRONMENT: Literal["dev", "production", "test"] = "dev"`
      - Field `LOG_LEVEL: Literal["DEBUG", "INFO", "WARNING", "ERROR"] = "INFO"`
      - Field `ALLOWED_ORIGINS: str = "http://localhost:3000"`
      - Field `APP_VERSION: str = "0.1.0"`
      - Property `allowed_origins_list` returning `[o.strip() for o in self.ALLOWED_ORIGINS.split(",") if o.strip()]`
  - Function `get_settings() -> Settings` decorated `@lru_cache` returning `Settings()` (with `# type: ignore[call-arg]` if pyright complains about required fields supplied via env)

Create `apps/api/src/database.py` reproducing RESEARCH.md Pattern 1 (lines 418-449) with these EXACT semantics:

  - `from __future__ import annotations`
  - Import `from collections.abc import AsyncIterator`
  - Import `from sqlalchemy.ext.asyncio import AsyncEngine, AsyncSession, async_sessionmaker, create_async_engine`
  - Import `from .config import get_settings`
  - Module-level: `settings = get_settings()` then `engine: AsyncEngine = create_async_engine(settings.DATABASE_URL, pool_pre_ping=True, pool_size=10, max_overflow=20, echo=False)`
  - Module-level: `AsyncSessionLocal = async_sessionmaker(engine, expire_on_commit=False, autoflush=False)` — `expire_on_commit=False` is mandatory per Anti-Pattern bullet (line 1033)
  - Async generator function `get_session() -> AsyncIterator[AsyncSession]` that yields from `AsyncSessionLocal()` inside `async with` with commit-on-success / rollback-on-exception pattern (verbatim per RESEARCH.md Pattern 1 lines 441-448)

Both files MUST type-check under pyright strict mode. Use full type annotations on every function and class. No bare returns, no `Any`.
  </action>

  <verify>
    <automated>test -f apps/api/src/__init__.py && test -f apps/api/src/modules/__init__.py && test -f apps/api/src/modules/health/__init__.py && grep -q 'class Settings(BaseSettings)' apps/api/src/config.py && grep -q 'DATABASE_URL: str' apps/api/src/config.py && grep -q 'SECRET_KEY: str' apps/api/src/config.py && grep -q 'ENVIRONMENT: Literal\["dev", "production", "test"\] = "dev"' apps/api/src/config.py && grep -q 'APP_VERSION: str = "0.1.0"' apps/api/src/config.py && grep -q 'def get_settings' apps/api/src/config.py && grep -q '@lru_cache' apps/api/src/config.py && grep -q 'create_async_engine' apps/api/src/database.py && grep -q 'pool_pre_ping=True' apps/api/src/database.py && grep -q 'AsyncSessionLocal = async_sessionmaker' apps/api/src/database.py && grep -q 'expire_on_commit=False' apps/api/src/database.py && grep -q 'async def get_session' apps/api/src/database.py && grep -q 'AsyncIterator\[AsyncSession\]' apps/api/src/database.py</automated>
  </verify>

  <acceptance_criteria>
    - `apps/api/src/__init__.py`, `apps/api/src/modules/__init__.py`, `apps/api/src/modules/health/__init__.py` all exist (empty acceptable)
    - `apps/api/src/config.py` defines `class Settings(BaseSettings)` with fields `DATABASE_URL: str`, `SECRET_KEY: str`, `ENVIRONMENT: Literal["dev","production","test"] = "dev"`, `LOG_LEVEL: Literal["DEBUG","INFO","WARNING","ERROR"] = "INFO"`, `ALLOWED_ORIGINS: str = "http://localhost:3000"`, `APP_VERSION: str = "0.1.0"`
    - `apps/api/src/config.py` defines `model_config = SettingsConfigDict(env_file=".env", env_file_encoding="utf-8", extra="ignore")` on the Settings class
    - `apps/api/src/config.py` defines `allowed_origins_list` as a `@property`
    - `apps/api/src/config.py` defines `get_settings()` decorated with `@lru_cache`
    - `apps/api/src/database.py` calls `create_async_engine` with `settings.DATABASE_URL`, `pool_pre_ping=True`, `pool_size=10`, `max_overflow=20`, `echo=False`
    - `apps/api/src/database.py` creates `AsyncSessionLocal = async_sessionmaker(engine, expire_on_commit=False, autoflush=False)` — `expire_on_commit=False` is non-negotiable
    - `apps/api/src/database.py` defines `async def get_session() -> AsyncIterator[AsyncSession]` with try/yield + commit on success + rollback in `except`
    - `from __future__ import annotations` is the first import in both `config.py` and `database.py`
    - No use of `Any`, no untyped function signatures (pyright strict compatible)
  </acceptance_criteria>

  <done>
    `from src.config import get_settings, Settings` and `from src.database import AsyncSessionLocal, engine, get_session` succeed when run from `apps/api/`. The conftest.py imports do not yet succeed (waiting on main.py + logging_config.py in Task 2/3).
  </done>
</task>

<task type="auto">
  <name>Task 2: structlog logging_config (configure_logging + RequestContextMiddleware + redactor)</name>
  <files>apps/api/src/logging_config.py</files>

  <read_first>
    - CLAUDE.md (line 138 — "Structured JSON logging"; lines 127-129 — `structlog>=24.4 JSON in prod / console in dev`)
    - .planning/phases/00-repo-skeleton/00-RESEARCH.md (Pattern 3 at lines 526-594 verbatim; Anti-Patterns line 1030; Pitfall 3 at lines 1090-1095)
    - apps/api/tests/test_logging.py (Plan 01 output — request_id contract)
  </read_first>

  <action>
Create `apps/api/src/logging_config.py` reproducing RESEARCH.md Pattern 3 (lines 528-594) with these EXACT semantics:

  - `from __future__ import annotations`
  - Imports: `logging`, `sys`, `uuid`, `structlog`, `from structlog.contextvars import bind_contextvars, clear_contextvars`, `from starlette.types import ASGIApp, Receive, Scope, Send`
  - Module-level constant `SENSITIVE_KEYS = {"password", "token", "secret", "authorization", "access_token", "refresh_token", "api_key", "secret_key"}` (lowercase-only entries; case-insensitive match in scrubber)
  - Function `_redact_processor(logger, method_name, event_dict)` — recursive scrubber: walks dict/list values; replaces `k` value with `"***"` if `k.lower() in SENSITIVE_KEYS`; returns the (possibly modified) event_dict. RESEARCH.md Pattern 3 lines 539-547 gives the exact recursion.
  - Function `configure_logging(environment: str, level: str) -> None` that:
      - Creates `timestamper = structlog.processors.TimeStamper(fmt="iso", utc=True)`
      - Builds `shared_processors = [merge_contextvars, add_log_level, StackInfoRenderer(), timestamper, _redact_processor]`
      - If `environment == "production"` use `JSONRenderer()`, else `ConsoleRenderer(colors=True)`
      - Calls `structlog.configure(processors=[*shared_processors, renderer], wrapper_class=structlog.make_filtering_bound_logger(getattr(logging, level)), logger_factory=structlog.PrintLoggerFactory(file=sys.stdout), cache_logger_on_first_use=True)`
  - Class `RequestContextMiddleware` — pure ASGI middleware (NOT subclassing BaseHTTPMiddleware — per RESEARCH.md Anti-Pattern at line 1030 and Pitfall 3 at lines 1090-1095, BaseHTTPMiddleware breaks contextvars). The class:
      - `__init__(self, app: ASGIApp) -> None: self.app = app`
      - `async __call__(self, scope: Scope, receive: Receive, send: Send) -> None:`
          - If `scope["type"] != "http"`, just delegate `await self.app(scope, receive, send); return`
          - Call `clear_contextvars()`
          - Look up `x-request-id` header in `scope.get("headers", [])` (headers are list of `(bytes, bytes)` tuples); fall back to `uuid.uuid4().hex` if missing
          - Call `bind_contextvars(request_id=request_id)`
          - `await self.app(scope, receive, send)`

The class MUST be defined exactly as written above. The Anti-Pattern note is non-negotiable — using `BaseHTTPMiddleware` will silently break request_id propagation across awaits.

Add a module-level docstring explaining the redactor's contract (drops `password`/`token`/`secret`/`authorization` keys at any nesting level). Add a class-level docstring on `RequestContextMiddleware` explaining "Pure ASGI; BaseHTTPMiddleware would break contextvars."

The module MUST type-check under pyright strict mode. The structlog processor signature `(logger, method_name, event_dict)` is implicitly Any-typed by structlog itself; use `from typing import Any` and annotate processor params as `Any` with a `# pyright: ignore` if strict mode complains — but prefer correct types from `structlog.types` (e.g., `WrappedLogger`, `EventDict`) if available.
  </action>

  <verify>
    <automated>grep -q 'class RequestContextMiddleware' apps/api/src/logging_config.py && ! grep -q 'BaseHTTPMiddleware' apps/api/src/logging_config.py && grep -q 'SENSITIVE_KEYS = {' apps/api/src/logging_config.py && grep -q '"password"' apps/api/src/logging_config.py && grep -q '"token"' apps/api/src/logging_config.py && grep -q '"secret"' apps/api/src/logging_config.py && grep -q '"authorization"' apps/api/src/logging_config.py && grep -q 'def configure_logging' apps/api/src/logging_config.py && grep -q 'def _redact_processor' apps/api/src/logging_config.py && grep -q 'JSONRenderer' apps/api/src/logging_config.py && grep -q 'ConsoleRenderer' apps/api/src/logging_config.py && grep -q 'bind_contextvars' apps/api/src/logging_config.py && grep -q 'clear_contextvars' apps/api/src/logging_config.py && grep -q 'x-request-id' apps/api/src/logging_config.py && grep -q 'uuid.uuid4' apps/api/src/logging_config.py</automated>
  </verify>

  <acceptance_criteria>
    - `apps/api/src/logging_config.py` exists
    - File defines `SENSITIVE_KEYS = {...}` containing at minimum: `"password"`, `"token"`, `"secret"`, `"authorization"`, `"access_token"`, `"refresh_token"`, `"api_key"`, `"secret_key"`
    - File defines `def _redact_processor(...)` that recurses into nested dicts and lists, replacing values whose keys (lowercased) are in `SENSITIVE_KEYS` with `"***"`
    - File defines `def configure_logging(environment: str, level: str) -> None` that switches between `JSONRenderer()` (when `environment == "production"`) and `ConsoleRenderer(colors=True)` (otherwise)
    - `configure_logging` includes `structlog.contextvars.merge_contextvars` and `_redact_processor` in the `shared_processors` list
    - File defines `class RequestContextMiddleware` with `__init__(self, app: ASGIApp)` and `async __call__(self, scope, receive, send)`
    - `RequestContextMiddleware` does NOT subclass `BaseHTTPMiddleware` (forbidden per RESEARCH.md Anti-Pattern + Pitfall 3 — verified by negative grep)
    - `__call__` checks `scope["type"] != "http"` and delegates to `self.app` without binding when not http
    - `__call__` calls `clear_contextvars()` before binding to prevent leakage across requests
    - `__call__` reads `x-request-id` header value (bytes) from `scope["headers"]` and falls back to `uuid.uuid4().hex` when absent
    - `__call__` calls `bind_contextvars(request_id=<value>)` before awaiting `self.app`
    - After Task 3 lands `src/main.py`, running `pytest apps/api/tests/test_logging.py::test_request_id_bound_via_middleware` (with deps installed) passes
  </acceptance_criteria>

  <done>
    `from src.logging_config import configure_logging, RequestContextMiddleware` succeeds. The class instantiates with any ASGI app and, when invoked with an HTTP scope, binds `request_id` to structlog contextvars (verifiable via `structlog.contextvars.get_contextvars()`).
  </done>
</task>

<task type="auto">
  <name>Task 3: FastAPI app (main.py) + health router (modules/health/router.py)</name>
  <files>apps/api/src/main.py, apps/api/src/modules/health/router.py</files>

  <read_first>
    - CLAUDE.md (lines 122-140 — FastAPI requirements; lines 132-138 — CORS, /health shape, lifespan, structured logging)
    - .planning/phases/00-repo-skeleton/00-RESEARCH.md (Pattern 1 main.py at lines 451-490; Pattern 2 health endpoint at lines 494-524; Don't Hand-Roll table "Health-check shape" at line 1057)
    - apps/api/src/config.py (just-created in Task 1 — Settings interface)
    - apps/api/src/database.py (just-created in Task 1 — engine, get_session)
    - apps/api/src/logging_config.py (just-created in Task 2 — configure_logging, RequestContextMiddleware)
    - apps/api/tests/test_health.py (Plan 01 output — contract for response shape)
  </read_first>

  <action>
Create `apps/api/src/modules/health/router.py` reproducing RESEARCH.md Pattern 2 (lines 496-523) with these EXACT semantics:

  - `from __future__ import annotations`
  - Imports: `from typing import Annotated`, `from fastapi import APIRouter, Depends`, `from sqlalchemy import text`, `from sqlalchemy.ext.asyncio import AsyncSession`, `from ...config import get_settings`, `from ...database import get_session`
  - Module-level: `router = APIRouter()` and `settings = get_settings()`
  - `@router.get("")` async function `get_health(session: Annotated[AsyncSession, Depends(get_session)]) -> dict[str, str | bool]`:
      - Wrap `result = await session.execute(text("SELECT 1"))` in try/except
      - `db_connected = result.scalar_one() == 1` on success; `False` on any exception
      - Return `{"status": "ok" if db_connected else "degraded", "version": settings.APP_VERSION, "environment": settings.ENVIRONMENT, "db_connected": db_connected}`
      - NOTE: when DB is down, return 200 with degraded status (NOT 500) — this matches k8s liveness convention per RESEARCH.md Don't Hand-Roll "Health-check shape" (line 1057) and the user constraint "Returns db_connected=false (not 500) if DB unreachable" from CLAUDE.md/planning_constraints

The endpoint path is empty string `""` because the router is included with `prefix="/health"` — final path resolves to `/health`. Function name is `get_health` per CLAUDE.md naming convention (lines 92-94 — `get_X`, never `fetch_X`/`load_X`/`retrieve_X`).

Create `apps/api/src/main.py` reproducing RESEARCH.md Pattern 1 (lines 451-490) with these EXACT semantics:

  - `from __future__ import annotations`
  - Imports: `from contextlib import asynccontextmanager`, `from collections.abc import AsyncIterator`, `from fastapi import FastAPI`, `from fastapi.middleware.cors import CORSMiddleware`, `from .config import get_settings`, `from .database import engine`, `from .logging_config import configure_logging, RequestContextMiddleware`, `from .modules.health.router import router as health_router`
  - Module-level: `settings = get_settings()` then `configure_logging(environment=settings.ENVIRONMENT, level=settings.LOG_LEVEL)`
  - `@asynccontextmanager` `async def lifespan(app: FastAPI) -> AsyncIterator[None]: yield; await engine.dispose()` — startup is implicit (engine module-level), shutdown disposes engine
  - `app = FastAPI(title="PRANAV API", version=settings.APP_VERSION, lifespan=lifespan)`
  - `app.add_middleware(CORSMiddleware, allow_origins=settings.allowed_origins_list, allow_credentials=True, allow_methods=["*"], allow_headers=["*"])`
  - `app.add_middleware(RequestContextMiddleware)` — add AFTER CORSMiddleware so request_id binds even for preflight responses
  - `app.include_router(health_router, prefix="/health", tags=["health"])`

CORS NOTE: `allow_credentials=True` combined with `allow_origins=["*"]` is forbidden by the CORS spec (FastAPI raises). The `allowed_origins_list` property splits the comma-separated `ALLOWED_ORIGINS` env var; default is `["http://localhost:3000"]`. Per RESEARCH.md Threat Pattern "CORS over-permissive" (line 1330) — never wildcard with credentials.

DO NOT add any other routers (auth, accounts, etc. — Phase 1+). DO NOT add exception handlers (Phase 1+). DO NOT add OpenAPI customization (default works).

Both files type-check under pyright strict. The router file imports `Annotated` from `typing` (not `typing_extensions`) since target is Python 3.12.
  </action>

  <verify>
    <automated>grep -q 'async def get_health' apps/api/src/modules/health/router.py && grep -q 'Annotated\[AsyncSession, Depends(get_session)\]' apps/api/src/modules/health/router.py && grep -q 'text("SELECT 1")' apps/api/src/modules/health/router.py && grep -q '"db_connected": db_connected' apps/api/src/modules/health/router.py && grep -q '"version": settings.APP_VERSION' apps/api/src/modules/health/router.py && grep -q '"environment": settings.ENVIRONMENT' apps/api/src/modules/health/router.py && grep -q '@router.get("")' apps/api/src/modules/health/router.py && grep -q 'FastAPI(' apps/api/src/main.py && grep -q 'title="PRANAV API"' apps/api/src/main.py && grep -q 'lifespan=lifespan' apps/api/src/main.py && grep -q '@asynccontextmanager' apps/api/src/main.py && grep -q 'await engine.dispose()' apps/api/src/main.py && grep -q 'CORSMiddleware' apps/api/src/main.py && grep -q 'RequestContextMiddleware' apps/api/src/main.py && grep -q 'include_router(health_router, prefix="/health"' apps/api/src/main.py && grep -q 'configure_logging' apps/api/src/main.py</automated>
  </verify>

  <acceptance_criteria>
    - `apps/api/src/modules/health/router.py` exists and defines `router = APIRouter()` at module level
    - Router defines `@router.get("")` returning a coroutine function named `get_health` (CLAUDE.md naming: `get_X`)
    - `get_health` accepts `session: Annotated[AsyncSession, Depends(get_session)]` parameter (FastAPI DI)
    - `get_health` executes `await session.execute(text("SELECT 1"))` inside try/except (DB failures fall back to `db_connected=False`, no 500)
    - `get_health` returns a dict with exactly four keys: `status`, `version`, `environment`, `db_connected` — and no others
    - `status` is `"ok"` when `db_connected` is True, `"degraded"` otherwise
    - `version` reads from `settings.APP_VERSION` (NOT a hardcoded string)
    - `environment` reads from `settings.ENVIRONMENT`
    - `apps/api/src/main.py` exists and instantiates `FastAPI` with `title="PRANAV API"`, `version=settings.APP_VERSION`, `lifespan=lifespan`
    - `main.py` calls `configure_logging(environment=settings.ENVIRONMENT, level=settings.LOG_LEVEL)` at module load
    - `main.py` defines `@asynccontextmanager async def lifespan(app: FastAPI) -> AsyncIterator[None]` that yields once and disposes `engine` on shutdown
    - `main.py` adds `CORSMiddleware` with `allow_origins=settings.allowed_origins_list`, `allow_credentials=True`, `allow_methods=["*"]`, `allow_headers=["*"]` — NEVER `allow_origins=["*"]` (forbidden combination per RESEARCH.md)
    - `main.py` adds `RequestContextMiddleware` AFTER `CORSMiddleware`
    - `main.py` calls `app.include_router(health_router, prefix="/health", tags=["health"])`
    - `main.py` defines NO other routers; defines NO exception handlers; defines NO additional endpoints (per CLAUDE.md "No feature routes beyond /health")
    - From repo root: `python -c "import sys; sys.path.insert(0, 'apps/api'); from src.main import app; print(app.title)"` prints `PRANAV API` (when deps are installed)
  </acceptance_criteria>

  <done>
    `from src.main import app` succeeds; `app` is a configured FastAPI instance with lifespan, CORS, request-context middleware, and the /health router. Plan 01's `apps/api/tests/conftest.py` imports now resolve. The `/health` endpoint will respond correctly once Wave 2's migration creates `_phase0_marker` and Wave 4 wires Postgres into docker-compose.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → api (CORS) | Browser sends Origin header; API validates against ALLOWED_ORIGINS allowlist |
| api → postgres | API uses asyncpg over Docker network; credentials from POSTGRES_PASSWORD env var |
| log sink (stdout/stderr) | Log lines may be ingested by external aggregators; sensitive fields must not appear in plain text |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-05 | Spoofing | CORS preflight — wildcard origin with credentials | mitigate | `main.py` uses `allow_origins=settings.allowed_origins_list` (parsed from env var); `allow_credentials=True` makes wildcard impossible (FastAPI raises). Default origin list `["http://localhost:3000"]` is explicit. Per RESEARCH.md V9 Communications + Threat Pattern "CORS over-permissive". |
| T-00-06 | Information Disclosure | Sensitive fields (password, token, authorization) appearing in structured log lines | mitigate | `_redact_processor` walks event_dict recursively and replaces values whose keys (lowercased) are in `SENSITIVE_KEYS` with `"***"`. Verified by Task 3 logging test in Plan 01. Per RESEARCH.md V7 Error Handling & Logging + Threat Pattern "Secret leakage via logs". |
| T-00-07 | Information Disclosure | /health response leaking internal state (DB DSN, secret hashes, internal IPs) | mitigate | Response is restricted to exactly 4 keys: `status`, `version`, `environment`, `db_connected`. No DB URL, no path, no auth state, no PII. Per RESEARCH.md Threat Pattern "/health endpoint information disclosure" (line 1334). |
| T-00-08 | Tampering | SQL injection via /health endpoint | accept | `/health` runs literal `text("SELECT 1")` with NO user input. No injection surface. Future routes with user input will use parameterized queries per SQLAlchemy convention (CLAUDE.md constraint #3). |
| T-00-09 | Denial of Service | /health endpoint becoming a DB-saturation vector under load | accept | Phase 0 is dev-only on localhost; no production deployment. Connection pool sized at `pool_size=10, max_overflow=20`. Rate-limiting deferred to v1.0 ship (per RESEARCH.md Deferred Ideas). |
| T-00-10 | Repudiation | Lack of request correlation across log lines | mitigate | `RequestContextMiddleware` binds `request_id` (from `x-request-id` header, or uuid4 fallback) to structlog contextvars before delegating to the app; `merge_contextvars` processor in `configure_logging` ensures every log line within the request scope carries the `request_id` field. Per RESEARCH.md Pattern 3 + Don't Hand-Roll "Request ID propagation". |

</threat_model>

<verification>
- `from src.main import app` succeeds; `app.title == "PRANAV API"`
- `app.routes` includes a GET on path `/health` (FastAPI auto-includes via `include_router(prefix="/health")` + `@router.get("")`)
- `RequestContextMiddleware` is NOT a subclass of `BaseHTTPMiddleware` (negative grep enforced in Task 2 verify)
- Once Wave 2's migration runs and the api container is up: `curl http://localhost:8000/health` returns HTTP 200 with `{"status":"ok","version":"0.1.0","environment":"dev","db_connected":true}` (asserted by Plan 01's `test_health_ok`)
- Once the FastAPI app handles a request with `x-request-id: abc123` header: the structlog log line for that request contains `"request_id":"abc123"` (asserted by Plan 01's `test_request_id_bound_via_middleware`)
</verification>

<success_criteria>
- All grep gates in `<verify>` pass for Tasks 1-3
- Plan 01's conftest import (`from src.main import app`) resolves
- `python -c "from src.main import app"` (with deps installed and DATABASE_URL set) succeeds without ImportError
- INFRA-02 and INFRA-07 are now satisfied in code; runtime verification waits on Wave 2 (Alembic baseline) and Wave 4 (docker-compose)
</success_criteria>

<output>
Create `.planning/phases/00-repo-skeleton/00-02-SUMMARY.md` when done. Record:
  - Files created (8 files)
  - Module export surface (`Settings`, `get_settings`, `engine`, `AsyncSessionLocal`, `get_session`, `configure_logging`, `RequestContextMiddleware`, `app`, `router`)
  - Confirmation that `RequestContextMiddleware` is pure ASGI (not BaseHTTPMiddleware)
  - Confirmation that `_redact_processor` covers all 8 SENSITIVE_KEYS entries
  - Any deviations from the RESEARCH.md patterns (with reason)
</output>
