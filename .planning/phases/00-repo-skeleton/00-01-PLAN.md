---
phase: 00-repo-skeleton
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - .gitignore
  - .gitattributes
  - .env.example
  - apps/api/.python-version
  - apps/api/requirements.txt
  - apps/api/requirements-dev.txt
  - apps/api/pyproject.toml
  - apps/api/tests/__init__.py
  - apps/api/tests/conftest.py
  - apps/api/tests/test_health.py
  - apps/api/tests/test_logging.py
  - apps/api/tests/test_env_example.py
autonomous: true
requirements:
  - INFRA-03
  - INFRA-06
  - INFRA-07

must_haves:
  truths:
    - "Repo root has .gitignore preventing accidental .env, node_modules, .next, __pycache__ commits"
    - ".env.example documents every required environment variable for docker compose to run"
    - "apps/api/pyproject.toml configures ruff, pyright, and pytest so make check has a valid target"
    - "apps/api/tests/conftest.py provides an async client fixture using ASGITransport + LifespanManager"
    - "apps/api/tests/test_health.py and test_logging.py exist with the assertions Wave 1 code must satisfy"
  artifacts:
    - path: ".gitignore"
      provides: "Excludes .env, __pycache__, .venv, node_modules, .next, .ruff_cache, .pytest_cache, .pyright_cache"
      contains: ".env"
    - path: ".env.example"
      provides: "Documents POSTGRES_USER, POSTGRES_PASSWORD, POSTGRES_DB, SECRET_KEY, ENVIRONMENT, LOG_LEVEL, ALLOWED_ORIGINS, NEXT_PUBLIC_API_URL"
      contains: "POSTGRES_USER=pranav"
    - path: "apps/api/requirements.txt"
      provides: "Runtime API dependencies pinned per RESEARCH.md Standard Stack"
      contains: "fastapi>=0.136,<0.137"
    - path: "apps/api/requirements-dev.txt"
      provides: "Test + tooling deps (pytest, anyio, httpx, asgi-lifespan, ruff, pyright)"
      contains: "asgi-lifespan>=2.1,<3"
    - path: "apps/api/pyproject.toml"
      provides: "ruff + pyright + pytest configuration (no build system)"
      contains: "[tool.pytest.ini_options]"
    - path: "apps/api/tests/conftest.py"
      provides: "Async client fixture using LifespanManager(app) + AsyncClient(ASGITransport(app=app))"
      contains: "LifespanManager"
    - path: "apps/api/tests/test_health.py"
      provides: "GET /health behavior assertion + baseline migration marker assertion"
      contains: "db_connected"
    - path: "apps/api/tests/test_logging.py"
      provides: "Asserts structlog JSON renderer in production env + request_id contextvar binding"
      contains: "request_id"
  key_links:
    - from: "apps/api/tests/conftest.py"
      to: "src.main:app"
      via: "import in fixture; LifespanManager wraps app for engine init"
      pattern: "from src.main import app"
    - from: "apps/api/pyproject.toml"
      to: "tests/ folder"
      via: "[tool.pytest.ini_options] testpaths = [\"tests\"]"
      pattern: "testpaths"

tags:
  - python
  - pytest
  - infra
  - bootstrap
---

<objective>
Establish Wave 0 test infrastructure and repo-level guards so all later waves have an executable feedback loop. Per the Nyquist mandate in `.planning/phases/00-repo-skeleton/00-VALIDATION.md`, no production code may ship before the tests that gate it exist.

Purpose: Wave 1 (FastAPI app) and Wave 2 (Alembic baseline) need:
  (a) a tests/ folder + conftest with async client + lifespan manager (httpx `AsyncClient` does NOT trigger ASGI lifespan on its own — see RESEARCH.md Pitfall 5),
  (b) a `make check`-compatible pyproject.toml,
  (c) a documented `.env.example` so docker compose interpolation in Wave 4 has known keys,
  (d) `.gitignore` so first commits don't accidentally include `.env` (RESEARCH.md Pitfall 8).

Tests created here will FAIL until Wave 1/2 land. That is intentional and per Wave 0 protocol.

Output: Repo root config (.gitignore, .gitattributes, .env.example) + apps/api scaffold (requirements files, pyproject.toml, tests/) + failing-by-design Phase 0 test suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/00-repo-skeleton/00-VALIDATION.md
@CLAUDE.md
@.planning/phases/00-repo-skeleton/00-RESEARCH.md

<interfaces>
<!-- These are the contracts Wave 1 code MUST satisfy. They are encoded in the test files this plan creates. -->
<!-- Executor must extract these from RESEARCH.md Pattern 5 and Pattern 3 verbatim. -->

GET /health response shape (Wave 1 must produce this):
{
  "status": "ok",            # str — "ok" if db_connected else "degraded"
  "version": "0.1.0",        # str — from Settings.APP_VERSION
  "environment": "dev",      # Literal["dev","production","test"]
  "db_connected": true       # bool — result of SELECT 1
}

structlog config (Wave 1 must export):
- configure_logging(environment: str, level: str) -> None
- class RequestContextMiddleware: pure ASGI; binds request_id to structlog.contextvars

Async test fixture pattern (this plan creates):
- from asgi_lifespan import LifespanManager
- from httpx import ASGITransport, AsyncClient
- async with LifespanManager(app):
-     async with AsyncClient(transport=ASGITransport(app=app), base_url="http://test") as ac:
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repo-root guards (.gitignore, .gitattributes, .env.example)</name>
  <files>.gitignore, .gitattributes, .env.example</files>

  <read_first>
    - CLAUDE.md (lines 1-200 — "What NOT to do", repo structure, env var list)
    - .planning/phases/00-repo-skeleton/00-RESEARCH.md (Pattern 9 ".env.example" at lines 910-932, Pitfall 8 at lines 1125-1130, Architectural Responsibility Map row "Secrets handling")
    - .planning/phases/00-repo-skeleton/00-VALIDATION.md (INFRA-06 entry)
  </read_first>

  <action>
Create `.gitignore` at repo root containing exactly these patterns (one per line, with leading comment headers for grouping):

  Section "Secrets":
    .env
    .env.*
    !.env.example

  Section "Python":
    __pycache__/
    *.pyc
    *.pyo
    .pytest_cache/
    .ruff_cache/
    .pyright/
    .pyrightconfig.json.bak
    .venv/
    venv/
    *.egg-info/
    .coverage
    htmlcov/

  Section "Node / Next.js":
    node_modules/
    .next/
    .turbo/
    .pnpm-store/
    *.tsbuildinfo
    next-env.d.ts

  Section "Editor / OS":
    .DS_Store
    .idea/
    .vscode/
    *.swp

  Section "Build artifacts":
    dist/
    build/

Create `.gitattributes` at repo root with a single rule: `* text=auto eol=lf` (forces LF line endings cross-platform; prevents Docker build differences between macOS dev and Linux containers).

Create `.env.example` at repo root with EXACTLY these key=value pairs (no quotes, no extra lines beyond grouping comments). Copy values verbatim from RESEARCH.md Pattern 9 (lines 910-932), substituting `pranav` for any `finbrain` legacy name. The required keys are:

  POSTGRES_USER=pranav
  POSTGRES_PASSWORD=pranav_dev_password
  POSTGRES_DB=pranav
  SECRET_KEY=change-me-in-prod-use-openssl-rand-hex-32
  ENVIRONMENT=dev
  LOG_LEVEL=INFO
  ALLOWED_ORIGINS=http://localhost:3000
  NEXT_PUBLIC_API_URL=http://localhost:8000

Include a leading comment block stating "NEVER COMMIT .env" and instructing the reader that `DATABASE_URL` is constructed by docker-compose from the POSTGRES_* variables (so it is intentionally NOT in this file as a separate entry — the docker-compose.yml interpolates it). For native-dev outside Docker, add a commented example `# DATABASE_URL=postgresql+asyncpg://pranav:pranav_dev_password@localhost:5432/pranav`.

Do NOT add any other variables; Phase 0 has no Plaid/SendGrid/S3 — those are Phase 9+ per RESEARCH.md Deferred Ideas (lines 65-77).
  </action>

  <verify>
    <automated>test "$(grep -c '^\.env$' .gitignore)" -ge 1 && test "$(grep -c '^!\.env\.example$' .gitignore)" -ge 1 && test "$(grep -c '^node_modules/$' .gitignore)" -ge 1 && test "$(grep -c '^\.next/$' .gitignore)" -ge 1 && test "$(grep -c '^__pycache__/$' .gitignore)" -ge 1 && grep -q 'POSTGRES_USER=pranav' .env.example && grep -q 'POSTGRES_DB=pranav' .env.example && grep -q 'SECRET_KEY=' .env.example && grep -q 'ENVIRONMENT=dev' .env.example && grep -q 'LOG_LEVEL=INFO' .env.example && grep -q 'ALLOWED_ORIGINS=http://localhost:3000' .env.example && grep -q 'NEXT_PUBLIC_API_URL=http://localhost:8000' .env.example && grep -q '* text=auto eol=lf' .gitattributes</automated>
  </verify>

  <acceptance_criteria>
    - `.gitignore` exists at repo root and contains literal lines: `.env`, `!.env.example`, `node_modules/`, `.next/`, `__pycache__/`, `.pytest_cache/`, `.ruff_cache/`, `.venv/`, `.DS_Store`
    - `.gitattributes` exists at repo root containing `* text=auto eol=lf`
    - `.env.example` exists at repo root containing literal substrings (one per line): `POSTGRES_USER=pranav`, `POSTGRES_PASSWORD=pranav_dev_password`, `POSTGRES_DB=pranav`, `SECRET_KEY=`, `ENVIRONMENT=dev`, `LOG_LEVEL=INFO`, `ALLOWED_ORIGINS=http://localhost:3000`, `NEXT_PUBLIC_API_URL=http://localhost:8000`
    - `.env.example` contains a leading comment line warning "NEVER COMMIT .env" (case-insensitive match acceptable)
    - `grep -E "DATABASE_URL=postgresql\\+asyncpg" .env.example` returns the commented native-dev example line
    - No file named `.env` is created (only `.env.example`); running `git status --ignored` after this task would list `.env` under ignored if a developer creates one
  </acceptance_criteria>

  <done>
    Repo-root guards are in place. Running `git status` would not flag `.env` accidentally if created, and the `.env.example` template lists every key the docker-compose.yml in Wave 4 will interpolate.
  </done>
</task>

<task type="auto">
  <name>Task 2: apps/api requirements files + pyproject.toml + .python-version</name>
  <files>apps/api/.python-version, apps/api/requirements.txt, apps/api/requirements-dev.txt, apps/api/pyproject.toml</files>

  <read_first>
    - CLAUDE.md (lines 122-140 — "FastAPI app requirements" — version floors; "Do not install any packages not listed above without asking")
    - .planning/phases/00-repo-skeleton/00-RESEARCH.md (Standard Stack Core API table at lines 108-126; Supporting (API test stack) at lines 127-135; Supporting (API tooling) at lines 137-143; Installation block at lines 191-216; Pitfall 10 at lines 1139-1144)
  </read_first>

  <action>
Create `apps/api/.python-version` containing exactly the single line: `3.12` (no trailing newline beyond standard POSIX-final-newline). This pins pyenv/asdf for editor tooling to match the python:3.12-slim Docker image and CI matrix (RESEARCH.md Pitfall 10).

Create `apps/api/requirements.txt` (RUNTIME deps) with EXACTLY these pinned entries, one per line, in this order. Versions are the floors verified against PyPI on 2026-05-23 per RESEARCH.md §Standard Stack Core (API):

  fastapi>=0.136,<0.137
  uvicorn[standard]>=0.36,<1
  sqlalchemy[asyncio]>=2.0.36,<2.1
  asyncpg>=0.30,<0.32
  alembic>=1.18,<2
  pydantic>=2.9,<3
  pydantic-settings>=2.5,<3
  structlog>=25.1,<26
  PyJWT>=2.9,<3
  pwdlib[argon2]>=0.3,<1
  python-multipart>=0.0.20,<1
  greenlet>=3.0,<4

PyJWT, pwdlib, python-multipart are installed in Phase 0 even though Phase 1 wires them — this avoids a mid-phase requirements bump per RESEARCH.md Locked Decisions line 30. Do NOT add `procrastinate` (RESEARCH.md Pitfall 6 — deferred to Phase 4). Do NOT add `cryptography` (deferred to Phase 9). Do NOT add `psycopg2-binary` unless Alembic raises a sync-context error during Wave 2 (RESEARCH.md alternatives note at lines 142-143).

Create `apps/api/requirements-dev.txt` (TEST + TOOLING deps) with EXACTLY these entries:

  -r requirements.txt
  pytest>=8,<10
  pytest-asyncio>=1.0,<2
  anyio>=4.0,<5
  asgi-lifespan>=2.1,<3
  httpx>=0.27,<1
  ruff>=0.15,<1
  pyright>=1.1.400,<2

The `-r requirements.txt` line means dev installs include runtime. `asgi-lifespan` is REQUIRED (not optional) because httpx `AsyncClient` does NOT trigger lifespan events (RESEARCH.md Pitfall 5).

Create `apps/api/pyproject.toml` with TOOL CONFIG ONLY (no `[build-system]`, no `[project]` metadata — the package is delivered as a directory under `apps/`, not as a wheel; CLAUDE.md mandates requirements.txt as source of truth). Required sections, written verbatim:

[tool.ruff]
line-length = 120
target-version = "py312"
src = ["src", "tests"]

[tool.ruff.lint]
# ruff "ALL" minus highly opinionated rules (RESEARCH.md Discretion line 59 recommendation)
select = ["E", "W", "F", "I", "B", "UP", "N", "S", "C4", "SIM", "PT", "RUF"]
ignore = ["S101"]   # allow assert in tests

[tool.ruff.lint.per-file-ignores]
"tests/*" = ["S101", "S105", "S106"]

[tool.pyright]
include = ["src", "tests"]
pythonVersion = "3.12"
typeCheckingMode = "strict"
reportMissingTypeStubs = false
venvPath = "."
venv = ".venv"

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
# anyio is the official FastAPI test backend; pytest-asyncio installed for flexibility
filterwarnings = [
  "error",
  "ignore::DeprecationWarning:asyncio",
]

NO `[build-system]` section. NO `[project]` section. NO setuptools/poetry/uv build metadata.
  </action>

  <verify>
    <automated>test "$(cat apps/api/.python-version)" = "3.12" && grep -q '^fastapi>=0.136,<0.137$' apps/api/requirements.txt && grep -q '^sqlalchemy\[asyncio\]>=2.0.36,<2.1$' apps/api/requirements.txt && grep -q '^asyncpg>=0.30,<0.32$' apps/api/requirements.txt && grep -q '^alembic>=1.18,<2$' apps/api/requirements.txt && grep -q '^structlog>=25.1,<26$' apps/api/requirements.txt && grep -q '^PyJWT>=2.9,<3$' apps/api/requirements.txt && grep -q '^pwdlib\[argon2\]>=0.3,<1$' apps/api/requirements.txt && ! grep -q '^procrastinate' apps/api/requirements.txt && ! grep -q '^cryptography' apps/api/requirements.txt && grep -q '^-r requirements.txt$' apps/api/requirements-dev.txt && grep -q '^asgi-lifespan>=2.1,<3$' apps/api/requirements-dev.txt && grep -q '^pytest>=8,<10$' apps/api/requirements-dev.txt && grep -q '^httpx>=0.27,<1$' apps/api/requirements-dev.txt && grep -q '^ruff>=0.15,<1$' apps/api/requirements-dev.txt && grep -q '^pyright>=1.1.400,<2$' apps/api/requirements-dev.txt && grep -q '\[tool.ruff\]' apps/api/pyproject.toml && grep -q '\[tool.pyright\]' apps/api/pyproject.toml && grep -q '\[tool.pytest.ini_options\]' apps/api/pyproject.toml && ! grep -q '\[build-system\]' apps/api/pyproject.toml && ! grep -q '\[project\]' apps/api/pyproject.toml</automated>
  </verify>

  <acceptance_criteria>
    - `apps/api/.python-version` contains exactly the line `3.12`
    - `apps/api/requirements.txt` contains exactly 12 dependency lines matching the order and pins above; no extra packages
    - `apps/api/requirements.txt` does NOT contain `procrastinate`, `cryptography`, `redis`, `celery`, `arq`, or `fastapi-users` (forbidden per RESEARCH.md Pitfall 6 + Anti-Patterns + What NOT to Use)
    - `apps/api/requirements-dev.txt` first line is `-r requirements.txt` followed by 7 dev/test deps
    - `apps/api/requirements-dev.txt` contains `asgi-lifespan>=2.1,<3` (REQUIRED per RESEARCH.md Pitfall 5 — non-optional)
    - `apps/api/pyproject.toml` contains sections `[tool.ruff]`, `[tool.ruff.lint]`, `[tool.pyright]`, `[tool.pytest.ini_options]`
    - `apps/api/pyproject.toml` does NOT contain `[build-system]` or `[project]` sections (CLAUDE.md mandates requirements.txt as source of truth)
    - `[tool.pyright]` block sets `typeCheckingMode = "strict"`
    - `[tool.pytest.ini_options]` block sets `asyncio_mode = "auto"` and `testpaths = ["tests"]`
  </acceptance_criteria>

  <done>
    apps/api/ has its dependency manifest and tool configuration. `pip install -r apps/api/requirements-dev.txt` (run inside the api container in Wave 4) will install everything needed for `ruff check`, `pyright`, and `pytest` to run.
  </done>
</task>

<task type="auto">
  <name>Task 3: Wave 0 failing-by-design test suite (conftest, test_health, test_logging, test_env_example)</name>
  <files>apps/api/tests/__init__.py, apps/api/tests/conftest.py, apps/api/tests/test_health.py, apps/api/tests/test_logging.py, apps/api/tests/test_env_example.py</files>

  <read_first>
    - CLAUDE.md (lines 122-140 — testing requirement; "Testing" section)
    - .planning/phases/00-repo-skeleton/00-RESEARCH.md (Pattern 5 pytest at lines 719-754; Pattern 3 structlog config at lines 526-594; Pitfall 5 at lines 1104-1110)
    - .planning/phases/00-repo-skeleton/00-VALIDATION.md (Per-Task Verification Map + Wave 0 Requirements)
  </read_first>

  <action>
Create `apps/api/tests/__init__.py` as an EMPTY file (pytest discovers tests via testpaths; the `__init__.py` makes `tests` a real Python package so pyright can resolve relative imports if needed).

Create `apps/api/tests/conftest.py` with exactly the fixture defined in RESEARCH.md Pattern 5 (lines 721-739). Reproduce these elements:

  - Import: `import pytest`
  - Import: `from asgi_lifespan import LifespanManager`
  - Import: `from httpx import ASGITransport, AsyncClient`
  - Import: `from src.main import app`  (Wave 1 must create src.main — this import will fail until Wave 1 lands; that is the intentional Wave 0 red signal)
  - Fixture `anyio_backend` returning `"asyncio"` (strict — not `"trio"`)
  - Async fixture `client` that wraps `LifespanManager(app)` around an `AsyncClient(transport=ASGITransport(app=app), base_url="http://test")`

LifespanManager is non-optional — without it the DB engine created in Wave 1's lifespan never initializes (RESEARCH.md Pitfall 5).

Create `apps/api/tests/test_health.py` with two tests (one per assertion path):

  Test 1: `test_health_ok(client)` — marked `@pytest.mark.anyio` — calls `await client.get("/health")`, asserts `resp.status_code == 200`, parses `body = resp.json()`, asserts:
    - `body["status"] == "ok"`
    - `body["db_connected"] is True`
    - `body["environment"] in {"dev","production","test"}`
    - `"version" in body`
    These assertions encode the INFRA-02 contract.

  Test 2: `test_baseline_migration_applied(client)` — marked `@pytest.mark.anyio` — uses the same async client to import a small helper that opens an `AsyncSession` from `src.database.AsyncSessionLocal`, runs `SELECT note FROM _phase0_marker WHERE id = 1`, asserts the row exists and the `note` text begins with `"Phase 0 migration succeeded"`. This encodes the INFRA-04 + INFRA-09 contract. Per RESEARCH.md Pattern 4 baseline migration (lines 656-715), the marker table is the load-bearing proof.
    The session import path is `from src.database import AsyncSessionLocal` and the query uses `from sqlalchemy import text`.

Create `apps/api/tests/test_logging.py` with one test:

  Test: `test_request_id_bound_via_middleware()` — NOT async. Imports `from src.logging_config import configure_logging, RequestContextMiddleware` and `from structlog.contextvars import get_contextvars`. Constructs a mock ASGI scope with `type="http"` and `headers=[(b"x-request-id", b"test-correlation-id")]`. Calls the middleware's `__call__` with a no-op `app` (an `async def passthrough(scope, receive, send): pass`), `receive` and `send` as `AsyncMock()` callables. After the call, asserts that `get_contextvars().get("request_id") == "test-correlation-id"`. This encodes the INFRA-07 contract.

Create `apps/api/tests/test_env_example.py` with one test:

  Test: `test_env_example_documents_all_required_keys()` — NOT async. Parses the repo-root `.env.example` (`Path(__file__).resolve().parents[3] / ".env.example"`), reads lines, builds a set of keys (left of `=`), asserts the set contains: `POSTGRES_USER`, `POSTGRES_PASSWORD`, `POSTGRES_DB`, `SECRET_KEY`, `ENVIRONMENT`, `LOG_LEVEL`, `ALLOWED_ORIGINS`, `NEXT_PUBLIC_API_URL`. This encodes the INFRA-06 contract.

All four test files MUST use `from __future__ import annotations` at the top (per pyright strict mode). Add type annotations to every fixture and helper. Run `ruff check apps/api/tests/` and `pyright apps/api/tests/` mentally as a self-check — both must be clean (they will fail to import `src.main` and `src.logging_config` until Wave 1 lands; that's expected as the Wave 0 red signal).

Do NOT skip or xfail any test — Wave 1 and Wave 2 will turn them green by creating `src/main.py`, `src/database.py`, `src/logging_config.py`, and the baseline migration.
  </action>

  <verify>
    <automated>test -f apps/api/tests/__init__.py && grep -q 'from asgi_lifespan import LifespanManager' apps/api/tests/conftest.py && grep -q 'ASGITransport' apps/api/tests/conftest.py && grep -q 'from src.main import app' apps/api/tests/conftest.py && grep -q 'def anyio_backend' apps/api/tests/conftest.py && grep -q 'def test_health_ok' apps/api/tests/test_health.py && grep -q 'db_connected.*True' apps/api/tests/test_health.py && grep -q 'def test_baseline_migration_applied' apps/api/tests/test_health.py && grep -q '_phase0_marker' apps/api/tests/test_health.py && grep -q 'def test_request_id_bound_via_middleware' apps/api/tests/test_logging.py && grep -q 'RequestContextMiddleware' apps/api/tests/test_logging.py && grep -q 'def test_env_example_documents_all_required_keys' apps/api/tests/test_env_example.py && grep -q 'POSTGRES_USER' apps/api/tests/test_env_example.py && grep -q 'NEXT_PUBLIC_API_URL' apps/api/tests/test_env_example.py</automated>
  </verify>

  <acceptance_criteria>
    - `apps/api/tests/__init__.py` exists (empty file is acceptable; 0 bytes is fine)
    - `apps/api/tests/conftest.py` imports `LifespanManager` from `asgi_lifespan`, `ASGITransport` and `AsyncClient` from `httpx`, and `app` from `src.main`
    - `apps/api/tests/conftest.py` defines a fixture named `anyio_backend` returning the literal string `"asyncio"`
    - `apps/api/tests/conftest.py` defines an async fixture named `client` that uses `LifespanManager(app)` as a context manager wrapping the `AsyncClient`
    - `apps/api/tests/test_health.py` defines `test_health_ok` decorated with `@pytest.mark.anyio` that asserts `body["status"] == "ok"`, `body["db_connected"] is True`, and `"version" in body`
    - `apps/api/tests/test_health.py` defines `test_baseline_migration_applied` that queries `_phase0_marker` table for `note` column and asserts text starts with `"Phase 0 migration succeeded"`
    - `apps/api/tests/test_logging.py` defines `test_request_id_bound_via_middleware` that imports `RequestContextMiddleware` from `src.logging_config` and asserts `get_contextvars()["request_id"] == "test-correlation-id"` after invocation with a scope containing an `x-request-id` header
    - `apps/api/tests/test_env_example.py` defines `test_env_example_documents_all_required_keys` that parses `.env.example` from the repo root and asserts presence of the 8 listed keys
    - All four test files import `from __future__ import annotations` as the first import after any module docstring
    - Running `pytest apps/api/tests/ --collect-only` (with deps installed) lists 4 tests across 3 files; collection succeeds (tests will fail at execution because Wave 1 hasn't shipped — that is expected and is the Wave 0 red signal)
    - Tests are NOT skipped, xfailed, or `pytest.mark.skip`'d
  </acceptance_criteria>

  <done>
    The Phase 0 test suite exists and encodes the INFRA-02, INFRA-04, INFRA-06, INFRA-07, INFRA-09 behavioral contracts. Wave 1 (FastAPI app) and Wave 2 (Alembic baseline) must satisfy these tests to be considered complete. Running `pytest apps/api/tests/ --collect-only` succeeds; running `pytest apps/api/tests/` fails because production code does not yet exist — exactly as Wave 0 protocol intends.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer-laptop ↔ git remote | Secrets (`.env`) must never cross this boundary; only `.env.example` template is committed |
| docker-compose env vars ↔ api container | Env vars are how secrets enter the runtime; no secrets are baked into the image |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-01 | Information Disclosure | `.env` file containing SECRET_KEY, POSTGRES_PASSWORD | mitigate | `.gitignore` Section "Secrets" includes `.env` and `.env.*` with `!.env.example` exception; verified by grep gate in Task 1 verify command |
| T-00-02 | Information Disclosure | `.env.example` template containing literal default secrets | mitigate | Template values are clearly-marked dev defaults (`change-me-in-prod-use-openssl-rand-hex-32`, `pranav_dev_password`); README in Plan 06 will warn against using these in production |
| T-00-03 | Tampering | Python dependencies from PyPI (supply chain) | mitigate | All 19 api packages passed slopcheck on 2026-05-23 per RESEARCH.md §Package Legitimacy Audit (lines 228-258); all packages have upper version bounds preventing accidental major-version drift |
| T-00-04 | Tampering | pytest-asyncio + anyio + asgi-lifespan + httpx + ruff + pyright (dev/test packages) | mitigate | All slopchecked [OK]; pinned in requirements-dev.txt with `<X` upper bounds |
| T-00-SC | Tampering | pip installs of fastapi, sqlalchemy, asyncpg, alembic, pydantic, pydantic-settings, structlog, PyJWT, pwdlib, python-multipart, greenlet, uvicorn (12 runtime + 7 dev = 19 packages) | mitigate | RESEARCH.md §Package Legitimacy Audit lists all 19 with [OK] verdicts; all are widely-used established packages with 3-200M monthly PyPI downloads; pinned with floor and ceiling versions. No new or LLM-suspect names. Per planner_authority_limits, no `[ASSUMED]` or `[SUS]` items to checkpoint — all packages are `[OK]` Approved. |

</threat_model>

<verification>
- `git status` does not show `.env` as a new file even if a developer creates one (the `.gitignore` ignores it)
- `apps/api/.python-version` matches `python:3.12-slim` in the Dockerfile (Wave 4) and `python-version: "3.12"` in CI (Plan 06)
- `pytest --collect-only` discovers 4 tests across 3 files (after Wave 4 installs deps); execution fails until Wave 1/2 ship
- Every required INFRA-06 env var is present in `.env.example` (Task 1 grep verify enforces)
- No forbidden packages (procrastinate, redis, cryptography, fastapi-users) appear in either requirements file (Task 2 grep verify enforces)
</verification>

<success_criteria>
- Plan 01 ships when all 3 tasks pass their automated `<verify>` commands.
- Wave 1 (Plan 02) has a non-empty test suite to satisfy; Plan 02's `make check` invocation (Plan 05) will turn green only when Wave 1 + Wave 2 production code is in place.
- INFRA-03 (`make check` passes), INFRA-06 (`.env.example` complete), and INFRA-07 (structlog contract encoded in test) are partially satisfied here; remaining satisfaction lands in Wave 1.
</success_criteria>

<output>
Create `.planning/phases/00-repo-skeleton/00-01-SUMMARY.md` when done. Record:
  - Files created (exact paths)
  - Test counts (4 tests, 3 test files + 1 conftest)
  - Which INFRA requirements are partially-satisfied here vs deferred to Wave 1/2
  - Any deviations from this plan (with reason)
</output>
